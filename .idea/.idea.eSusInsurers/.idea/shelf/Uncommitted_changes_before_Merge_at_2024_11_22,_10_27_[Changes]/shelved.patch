Index: eSusInsurers/ConfigServices/ControllerHelper.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using Microsoft.AspNetCore.Mvc.Infrastructure;\nusing Microsoft.AspNetCore.Mvc;\nusing eSusInsurers.Policy;\n\nnamespace eSusInsurers.ConfigServices\n{\n    /// <summary>\n    /// Helper class for configuring controller-related services.\n    /// </summary>\n    public static class ControllerHelper\n    {\n        /// <summary>\n        /// Adds controller-related services to the specified <see cref=\"IServiceCollection\"/>.\n        /// </summary>\n        /// <param name=\"services\">The <see cref=\"IServiceCollection\"/> to add the services to.</param>\n        /// <param name=\"configuration\">The configuration.</param>\n        /// <returns>The modified <see cref=\"IServiceCollection\"/>.</returns>\n        public static IServiceCollection AddControllerServices(this IServiceCollection services, IConfiguration configuration)\n        {\n            services.AddControllers()\n                .AddSerializerConfiguration()\n                .ConfigureApiBehaviorOptions(options =>\n                {\n                    // Replace the built-in ASP.NET InvalidModelStateResponse to use our custom response code with 422\n                    options.InvalidModelStateResponseFactory = context =>\n                    {\n                        var problemDetailsFactory = context.HttpContext.RequestServices.GetRequiredService<ProblemDetailsFactory>();\n                        var problemDetails = problemDetailsFactory.CreateValidationProblemDetails(context.HttpContext, context.ModelState, statusCode: 422);\n                        var result = new UnprocessableEntityObjectResult(problemDetails);\n                        result.ContentTypes.Add(\"application/problem+json\");\n                        return result;\n                    };\n                })\n                .AddJsonOptions(x =>\n                {\n                    x.JsonSerializerOptions.PropertyNamingPolicy = new SnakeCaseNamingPolicy();\n                });\n\n            //services.AddSingleton<ICurrentUserService, CurrentUserService>();\n            //services.AddSingleton<IIdentityService, IdentityService>();\n\n            services.AddHttpContextAccessor();\n\n            return services;\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/ConfigServices/ControllerHelper.cs b/eSusInsurers/ConfigServices/ControllerHelper.cs
--- a/eSusInsurers/ConfigServices/ControllerHelper.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/ConfigServices/ControllerHelper.cs	(date 1732264029103)
@@ -1,47 +1,47 @@
-using Microsoft.AspNetCore.Mvc.Infrastructure;
-using Microsoft.AspNetCore.Mvc;
 using eSusInsurers.Policy;
+using Microsoft.AspNetCore.Mvc;
+using Microsoft.AspNetCore.Mvc.Infrastructure;
 
-namespace eSusInsurers.ConfigServices
-{
-    /// <summary>
-    /// Helper class for configuring controller-related services.
-    /// </summary>
-    public static class ControllerHelper
-    {
-        /// <summary>
-        /// Adds controller-related services to the specified <see cref="IServiceCollection"/>.
-        /// </summary>
-        /// <param name="services">The <see cref="IServiceCollection"/> to add the services to.</param>
-        /// <param name="configuration">The configuration.</param>
-        /// <returns>The modified <see cref="IServiceCollection"/>.</returns>
-        public static IServiceCollection AddControllerServices(this IServiceCollection services, IConfiguration configuration)
-        {
-            services.AddControllers()
-                .AddSerializerConfiguration()
-                .ConfigureApiBehaviorOptions(options =>
-                {
-                    // Replace the built-in ASP.NET InvalidModelStateResponse to use our custom response code with 422
-                    options.InvalidModelStateResponseFactory = context =>
-                    {
-                        var problemDetailsFactory = context.HttpContext.RequestServices.GetRequiredService<ProblemDetailsFactory>();
-                        var problemDetails = problemDetailsFactory.CreateValidationProblemDetails(context.HttpContext, context.ModelState, statusCode: 422);
-                        var result = new UnprocessableEntityObjectResult(problemDetails);
-                        result.ContentTypes.Add("application/problem+json");
-                        return result;
-                    };
-                })
-                .AddJsonOptions(x =>
-                {
-                    x.JsonSerializerOptions.PropertyNamingPolicy = new SnakeCaseNamingPolicy();
-                });
+namespace eSusInsurers.ConfigServices;
+
+/// <summary>
+///     Helper class for configuring controller-related services.
+/// </summary>
+public static class ControllerHelper
+{
+    /// <summary>
+    ///     Adds controller-related services to the specified <see cref="IServiceCollection" />.
+    /// </summary>
+    /// <param name="services">The <see cref="IServiceCollection" /> to add the services to.</param>
+    /// <param name="configuration">The configuration.</param>
+    /// <returns>The modified <see cref="IServiceCollection" />.</returns>
+    public static IServiceCollection AddControllerServices(this IServiceCollection services,
+        IConfiguration configuration)
+    {
+        services.AddControllers()
+            .AddSerializerConfiguration()
+            .ConfigureApiBehaviorOptions(options =>
+            {
+                // Replace the built-in ASP.NET InvalidModelStateResponse to use our custom response code with 422
+                options.InvalidModelStateResponseFactory = context =>
+                {
+                    var problemDetailsFactory =
+                        context.HttpContext.RequestServices.GetRequiredService<ProblemDetailsFactory>();
+                    var problemDetails =
+                        problemDetailsFactory.CreateValidationProblemDetails(context.HttpContext, context.ModelState,
+                            422);
+                    var result = new UnprocessableEntityObjectResult(problemDetails);
+                    result.ContentTypes.Add("application/problem+json");
+                    return result;
+                };
+            })
+            .AddJsonOptions(x => { x.JsonSerializerOptions.PropertyNamingPolicy = new SnakeCaseNamingPolicy(); });
 
-            //services.AddSingleton<ICurrentUserService, CurrentUserService>();
-            //services.AddSingleton<IIdentityService, IdentityService>();
+        //services.AddSingleton<ICurrentUserService, CurrentUserService>();
+        //services.AddSingleton<IIdentityService, IdentityService>();
 
-            services.AddHttpContextAccessor();
+        services.AddHttpContextAccessor();
 
-            return services;
-        }
+        return services;
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/TokenService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using eSusInsurers.Common.Exceptions;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Models.Users.Login;\nusing eSusInsurers.Models.Users.RefreshAccessToken;\nusing eSusInsurers.Services.Interfaces;\nusing Microsoft.IdentityModel.Tokens;\nusing System.IdentityModel.Tokens.Jwt;\nusing System.Security.Claims;\nusing System.Security.Cryptography;\nusing System.Text;\n\nnamespace eSusInsurers.Services.Implementations\n{\n    public class TokenService : ITokenService\n    {\n        #region Fields\n        private IUnitOfWork _unitOfWork;\n        private readonly IConfiguration _configuration;\n        #endregion\n\n        #region Constructor\n        public TokenService(IUnitOfWork unitOfWork, IConfiguration configuration)\n        {\n            _unitOfWork = unitOfWork;\n            _configuration = configuration;\n        } \n        #endregion\n\n        public string GenerateAccessToken(IEnumerable<Claim> claims)\n        {\n            var secretKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration[\"JWT:Secret\"]));\n            var signinCredentials = new SigningCredentials(secretKey, SecurityAlgorithms.HmacSha256);\n\n            _ = int.TryParse(_configuration[\"JWT:TokenValidityInMinutes\"], out int tokenValidityInMinutes);\n\n            var tokeOptions = new JwtSecurityToken(\n                issuer: _configuration[\"JWT:ValidAudience\"],\n                audience: _configuration[\"JWT:ValidIssuer\"],\n                claims: claims,\n                expires: DateTime.UtcNow.AddMinutes(tokenValidityInMinutes),\n                signingCredentials: signinCredentials\n            );\n\n            var tokenString = new JwtSecurityTokenHandler().WriteToken(tokeOptions);\n            return tokenString;\n        }\n\n        public string GenerateRefreshToken()\n        {\n            var randomNumber = new byte[32];\n            using (var rng = RandomNumberGenerator.Create())\n            {\n                rng.GetBytes(randomNumber);\n                return Convert.ToBase64String(randomNumber);\n            }\n        }\n\n        public ClaimsPrincipal GetPrincipalFromExpiredToken(string token)\n        {\n            var tokenValidationParameters = new TokenValidationParameters\n            {\n                ValidateAudience = false, //you might want to validate the audience and issuer depending on your use case\n                ValidateIssuer = false,\n                ValidateIssuerSigningKey = true,\n                IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration[\"JWT:Secret\"])),\n                ValidateLifetime = false //here we are saying that we don't care about the token's expiration date\n            };\n\n            var tokenHandler = new JwtSecurityTokenHandler();\n            SecurityToken securityToken;\n            var principal = tokenHandler.ValidateToken(token, tokenValidationParameters, out securityToken);\n            var jwtSecurityToken = securityToken as JwtSecurityToken;\n            if (jwtSecurityToken == null || !jwtSecurityToken.Header.Alg.Equals(SecurityAlgorithms.HmacSha256, StringComparison.InvariantCultureIgnoreCase))\n                throw new SecurityTokenException(\"Invalid token\");\n\n            return principal;\n        }\n\n        public async Task<object> RefreshToken(TokenApiModel tokenApiModel, CancellationToken cancellationToken = default)\n        {\n            ArgumentNullException.ThrowIfNull(tokenApiModel, nameof(tokenApiModel));\n\n            ArgumentNullException.ThrowIfNull(tokenApiModel.RefreshToken, nameof(tokenApiModel.RefreshToken));\n            \n            ArgumentNullException.ThrowIfNull(tokenApiModel.AccessToken, nameof(tokenApiModel.AccessToken));\n\n            string accessToken = tokenApiModel.AccessToken;\n            \n            string refreshToken = tokenApiModel.RefreshToken;\n            \n            var principal = GetPrincipalFromExpiredToken(accessToken);\n\n            var userName = principal.Identity.Name; //this is mapped to the Name claim by default\n\n            var user = await _unitOfWork.UserRepository.GetByEmailIdAsync(userName, cancellationToken);\n            \n            if (user is null || user.RefreshToken != refreshToken || user.RefreshTokenExpiryTime <= DateTime.UtcNow)\n                throw new BadRequestException(\"Invalid client request\");\n            \n            var newAccessToken = GenerateAccessToken(principal.Claims);\n            \n            var newRefreshToken = GenerateRefreshToken();\n\n            user.RefreshToken = newRefreshToken;\n\n            _ = int.TryParse(_configuration[\"JWT:RefreshTokenValidityInDays\"], out int refreshTokenValidityInDays);\n\n            user.RefreshTokenExpiryTime = DateTime.UtcNow.AddDays(refreshTokenValidityInDays);\n\n            var transaction = await _unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                await _unitOfWork.UserRepository.UpdateAsync(user, cancellationToken);\n\n                await _unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync();\n            }\n            catch (Exception ex)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n\n            return new AuthenticatedResponse()\n            {\n                Token = newAccessToken,\n                RefreshToken = newRefreshToken\n            };\n        }\n\n        public async Task<bool> RevokeToken(string userName, CancellationToken cancellationToken = default)\n        {          \n            var user = await _unitOfWork.UserRepository.GetByEmailIdAsync(userName, cancellationToken);\n\n            if (user is null)\n                throw new BadRequestException(\"Invalid user.\");\n\n            user.RefreshToken = null;\n\n            var transaction = await _unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                await _unitOfWork.UserRepository.UpdateAsync(user, cancellationToken);\n\n                await _unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync();\n            }\n            catch (Exception ex)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n\n            return true;\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/TokenService.cs b/eSusInsurers/Services/Implementations/TokenService.cs
--- a/eSusInsurers/Services/Implementations/TokenService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/TokenService.cs	(date 1732264029127)
@@ -1,163 +1,167 @@
+using System.IdentityModel.Tokens.Jwt;
+using System.Security.Claims;
+using System.Security.Cryptography;
+using System.Text;
 using eSusInsurers.Common.Exceptions;
 using eSusInsurers.Infrastructure.Common;
 using eSusInsurers.Models.Users.Login;
 using eSusInsurers.Models.Users.RefreshAccessToken;
 using eSusInsurers.Services.Interfaces;
 using Microsoft.IdentityModel.Tokens;
-using System.IdentityModel.Tokens.Jwt;
-using System.Security.Claims;
-using System.Security.Cryptography;
-using System.Text;
 
-namespace eSusInsurers.Services.Implementations
-{
-    public class TokenService : ITokenService
-    {
-        #region Fields
-        private IUnitOfWork _unitOfWork;
-        private readonly IConfiguration _configuration;
-        #endregion
-
-        #region Constructor
-        public TokenService(IUnitOfWork unitOfWork, IConfiguration configuration)
-        {
-            _unitOfWork = unitOfWork;
-            _configuration = configuration;
-        } 
-        #endregion
+namespace eSusInsurers.Services.Implementations;
+
+public class TokenService : ITokenService
+{
+    #region Constructor
+
+    public TokenService(IUnitOfWork unitOfWork, IConfiguration configuration)
+    {
+        _unitOfWork = unitOfWork;
+        _configuration = configuration;
+    }
+
+    #endregion
 
-        public string GenerateAccessToken(IEnumerable<Claim> claims)
-        {
-            var secretKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration["JWT:Secret"]));
-            var signinCredentials = new SigningCredentials(secretKey, SecurityAlgorithms.HmacSha256);
+    public string GenerateAccessToken(IEnumerable<Claim> claims)
+    {
+        var secretKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration["JWT:Secret"]));
+        var signinCredentials = new SigningCredentials(secretKey, SecurityAlgorithms.HmacSha256);
 
-            _ = int.TryParse(_configuration["JWT:TokenValidityInMinutes"], out int tokenValidityInMinutes);
+        _ = int.TryParse(_configuration["JWT:TokenValidityInMinutes"], out var tokenValidityInMinutes);
 
-            var tokeOptions = new JwtSecurityToken(
-                issuer: _configuration["JWT:ValidAudience"],
-                audience: _configuration["JWT:ValidIssuer"],
-                claims: claims,
-                expires: DateTime.UtcNow.AddMinutes(tokenValidityInMinutes),
-                signingCredentials: signinCredentials
-            );
+        var tokeOptions = new JwtSecurityToken(
+            _configuration["JWT:ValidAudience"],
+            _configuration["JWT:ValidIssuer"],
+            claims,
+            expires: DateTime.UtcNow.AddMinutes(tokenValidityInMinutes),
+            signingCredentials: signinCredentials
+        );
 
-            var tokenString = new JwtSecurityTokenHandler().WriteToken(tokeOptions);
-            return tokenString;
-        }
+        var tokenString = new JwtSecurityTokenHandler().WriteToken(tokeOptions);
+        return tokenString;
+    }
 
-        public string GenerateRefreshToken()
-        {
-            var randomNumber = new byte[32];
-            using (var rng = RandomNumberGenerator.Create())
-            {
-                rng.GetBytes(randomNumber);
-                return Convert.ToBase64String(randomNumber);
-            }
-        }
+    public string GenerateRefreshToken()
+    {
+        var randomNumber = new byte[32];
+        using (var rng = RandomNumberGenerator.Create())
+        {
+            rng.GetBytes(randomNumber);
+            return Convert.ToBase64String(randomNumber);
+        }
+    }
 
-        public ClaimsPrincipal GetPrincipalFromExpiredToken(string token)
-        {
-            var tokenValidationParameters = new TokenValidationParameters
-            {
-                ValidateAudience = false, //you might want to validate the audience and issuer depending on your use case
-                ValidateIssuer = false,
-                ValidateIssuerSigningKey = true,
-                IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration["JWT:Secret"])),
-                ValidateLifetime = false //here we are saying that we don't care about the token's expiration date
-            };
+    public ClaimsPrincipal GetPrincipalFromExpiredToken(string token)
+    {
+        var tokenValidationParameters = new TokenValidationParameters
+        {
+            ValidateAudience = false, //you might want to validate the audience and issuer depending on your use case
+            ValidateIssuer = false,
+            ValidateIssuerSigningKey = true,
+            IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration["JWT:Secret"])),
+            ValidateLifetime = false //here we are saying that we don't care about the token's expiration date
+        };
 
-            var tokenHandler = new JwtSecurityTokenHandler();
-            SecurityToken securityToken;
-            var principal = tokenHandler.ValidateToken(token, tokenValidationParameters, out securityToken);
-            var jwtSecurityToken = securityToken as JwtSecurityToken;
-            if (jwtSecurityToken == null || !jwtSecurityToken.Header.Alg.Equals(SecurityAlgorithms.HmacSha256, StringComparison.InvariantCultureIgnoreCase))
-                throw new SecurityTokenException("Invalid token");
+        var tokenHandler = new JwtSecurityTokenHandler();
+        SecurityToken securityToken;
+        var principal = tokenHandler.ValidateToken(token, tokenValidationParameters, out securityToken);
+        var jwtSecurityToken = securityToken as JwtSecurityToken;
+        if (jwtSecurityToken == null || !jwtSecurityToken.Header.Alg.Equals(SecurityAlgorithms.HmacSha256,
+                StringComparison.InvariantCultureIgnoreCase))
+            throw new SecurityTokenException("Invalid token");
 
-            return principal;
-        }
+        return principal;
+    }
 
-        public async Task<object> RefreshToken(TokenApiModel tokenApiModel, CancellationToken cancellationToken = default)
-        {
-            ArgumentNullException.ThrowIfNull(tokenApiModel, nameof(tokenApiModel));
+    public async Task<object> RefreshToken(TokenApiModel tokenApiModel, CancellationToken cancellationToken = default)
+    {
+        ArgumentNullException.ThrowIfNull(tokenApiModel, nameof(tokenApiModel));
 
-            ArgumentNullException.ThrowIfNull(tokenApiModel.RefreshToken, nameof(tokenApiModel.RefreshToken));
-            
-            ArgumentNullException.ThrowIfNull(tokenApiModel.AccessToken, nameof(tokenApiModel.AccessToken));
+        ArgumentNullException.ThrowIfNull(tokenApiModel.RefreshToken, nameof(tokenApiModel.RefreshToken));
+
+        ArgumentNullException.ThrowIfNull(tokenApiModel.AccessToken, nameof(tokenApiModel.AccessToken));
 
-            string accessToken = tokenApiModel.AccessToken;
-            
-            string refreshToken = tokenApiModel.RefreshToken;
-            
-            var principal = GetPrincipalFromExpiredToken(accessToken);
+        var accessToken = tokenApiModel.AccessToken;
+
+        var refreshToken = tokenApiModel.RefreshToken;
+
+        var principal = GetPrincipalFromExpiredToken(accessToken);
 
-            var userName = principal.Identity.Name; //this is mapped to the Name claim by default
+        var userName = principal.Identity.Name; //this is mapped to the Name claim by default
 
-            var user = await _unitOfWork.UserRepository.GetByEmailIdAsync(userName, cancellationToken);
-            
-            if (user is null || user.RefreshToken != refreshToken || user.RefreshTokenExpiryTime <= DateTime.UtcNow)
-                throw new BadRequestException("Invalid client request");
-            
-            var newAccessToken = GenerateAccessToken(principal.Claims);
-            
-            var newRefreshToken = GenerateRefreshToken();
+        var user = await _unitOfWork.UserRepository.GetByEmailIdAsync(userName, cancellationToken);
+
+        if (user is null || user.RefreshToken != refreshToken || user.RefreshTokenExpiryTime <= DateTime.UtcNow)
+            throw new BadRequestException("Invalid client request");
+
+        var newAccessToken = GenerateAccessToken(principal.Claims);
+
+        var newRefreshToken = GenerateRefreshToken();
 
-            user.RefreshToken = newRefreshToken;
+        user.RefreshToken = newRefreshToken;
 
-            _ = int.TryParse(_configuration["JWT:RefreshTokenValidityInDays"], out int refreshTokenValidityInDays);
+        _ = int.TryParse(_configuration["JWT:RefreshTokenValidityInDays"], out var refreshTokenValidityInDays);
 
-            user.RefreshTokenExpiryTime = DateTime.UtcNow.AddDays(refreshTokenValidityInDays);
+        user.RefreshTokenExpiryTime = DateTime.UtcNow.AddDays(refreshTokenValidityInDays);
 
-            var transaction = await _unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await _unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                await _unitOfWork.UserRepository.UpdateAsync(user, cancellationToken);
+        try
+        {
+            await _unitOfWork.UserRepository.UpdateAsync(user, cancellationToken);
 
-                await _unitOfWork.SaveChangesAsync(cancellationToken);
+            await _unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync();
-            }
-            catch (Exception ex)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            await transaction.CommitAsync();
+        }
+        catch (Exception ex)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
+            throw;
+        }
 
-            return new AuthenticatedResponse()
-            {
-                Token = newAccessToken,
-                RefreshToken = newRefreshToken
-            };
-        }
+        return new AuthenticatedResponse
+        {
+            Token = newAccessToken,
+            RefreshToken = newRefreshToken
+        };
+    }
 
-        public async Task<bool> RevokeToken(string userName, CancellationToken cancellationToken = default)
-        {          
-            var user = await _unitOfWork.UserRepository.GetByEmailIdAsync(userName, cancellationToken);
+    public async Task<bool> RevokeToken(string userName, CancellationToken cancellationToken = default)
+    {
+        var user = await _unitOfWork.UserRepository.GetByEmailIdAsync(userName, cancellationToken);
 
-            if (user is null)
-                throw new BadRequestException("Invalid user.");
+        if (user is null)
+            throw new BadRequestException("Invalid user.");
 
-            user.RefreshToken = null;
+        user.RefreshToken = null;
 
-            var transaction = await _unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await _unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                await _unitOfWork.UserRepository.UpdateAsync(user, cancellationToken);
+        try
+        {
+            await _unitOfWork.UserRepository.UpdateAsync(user, cancellationToken);
 
-                await _unitOfWork.SaveChangesAsync(cancellationToken);
+            await _unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync();
-            }
-            catch (Exception ex)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            await transaction.CommitAsync();
+        }
+        catch (Exception ex)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
+            throw;
+        }
 
-            return true;
-        }
+        return true;
     }
-}
+
+    #region Fields
+
+    private readonly IUnitOfWork _unitOfWork;
+    private readonly IConfiguration _configuration;
+
+    #endregion
+}
\ No newline at end of file
Index: eSusInsurers/ConfigServices/CustomNamingPolicy.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using System.Text.Json;\n\nnamespace eSusInsurers.ConfigServices\n{\n    /// <summary>\n    /// \n    /// </summary>\n    public class CustomNamingPolicy : JsonNamingPolicy\n    {\n        /// <summary>\n        /// \n        /// </summary>\n        /// <param name=\"name\"></param>\n        /// <returns></returns>\n        public override string ConvertName(string name)\n        {\n            // Convert the property name to match the JSON property name\n            switch (name)\n            {\n                case \"TotalValues\":\n                    return \"total_values\";\n                case \"LocationCode\":\n                    return \"location_code\";\n                case \"ManufacturerCode\":\n                    return \"manufacturer_code\";\n                case \"PartDescription\":\n                    return \"part_description\";\n                case \"GenericPartNumber\":\n                    return \"generic_part_number\";\n                case \"VendorNumber\":\n                    return \"vendor_number\";\n                case \"VendorName\":\n                    return \"vendor_name\";\n                case \"TotalOnhandCost\":\n                    return \"total_onhand_cost\";\n                case \"MinQuantity\":\n                    return \"min_quantity\";\n                case \"MaxQuantity\":\n                    return \"max_quantity\";\n                case \"CurrentAverageCost\":\n                    return \"current_average_cost\";\n                case \"BinNumber1\":\n                    return \"bin_number_1\";\n                case \"BinNumber2\":\n                    return \"bin_number_2\";\n                case \"BinNumber3\":\n                    return \"bin_number_3\";\n                default:\n                    return name;\n            }\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/ConfigServices/CustomNamingPolicy.cs b/eSusInsurers/ConfigServices/CustomNamingPolicy.cs
--- a/eSusInsurers/ConfigServices/CustomNamingPolicy.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/ConfigServices/CustomNamingPolicy.cs	(date 1732264029100)
@@ -1,53 +1,50 @@
 using System.Text.Json;
 
-namespace eSusInsurers.ConfigServices
-{
-    /// <summary>
-    /// 
-    /// </summary>
-    public class CustomNamingPolicy : JsonNamingPolicy
-    {
-        /// <summary>
-        /// 
-        /// </summary>
-        /// <param name="name"></param>
-        /// <returns></returns>
-        public override string ConvertName(string name)
-        {
-            // Convert the property name to match the JSON property name
-            switch (name)
-            {
-                case "TotalValues":
-                    return "total_values";
-                case "LocationCode":
-                    return "location_code";
-                case "ManufacturerCode":
-                    return "manufacturer_code";
-                case "PartDescription":
-                    return "part_description";
-                case "GenericPartNumber":
-                    return "generic_part_number";
-                case "VendorNumber":
-                    return "vendor_number";
-                case "VendorName":
-                    return "vendor_name";
-                case "TotalOnhandCost":
-                    return "total_onhand_cost";
-                case "MinQuantity":
-                    return "min_quantity";
-                case "MaxQuantity":
-                    return "max_quantity";
-                case "CurrentAverageCost":
-                    return "current_average_cost";
-                case "BinNumber1":
-                    return "bin_number_1";
-                case "BinNumber2":
-                    return "bin_number_2";
-                case "BinNumber3":
-                    return "bin_number_3";
-                default:
-                    return name;
-            }
+namespace eSusInsurers.ConfigServices;
+
+/// <summary>
+/// </summary>
+public class CustomNamingPolicy : JsonNamingPolicy
+{
+    /// <summary>
+    /// </summary>
+    /// <param name="name"></param>
+    /// <returns></returns>
+    public override string ConvertName(string name)
+    {
+        // Convert the property name to match the JSON property name
+        switch (name)
+        {
+            case "TotalValues":
+                return "total_values";
+            case "LocationCode":
+                return "location_code";
+            case "ManufacturerCode":
+                return "manufacturer_code";
+            case "PartDescription":
+                return "part_description";
+            case "GenericPartNumber":
+                return "generic_part_number";
+            case "VendorNumber":
+                return "vendor_number";
+            case "VendorName":
+                return "vendor_name";
+            case "TotalOnhandCost":
+                return "total_onhand_cost";
+            case "MinQuantity":
+                return "min_quantity";
+            case "MaxQuantity":
+                return "max_quantity";
+            case "CurrentAverageCost":
+                return "current_average_cost";
+            case "BinNumber1":
+                return "bin_number_1";
+            case "BinNumber2":
+                return "bin_number_2";
+            case "BinNumber3":
+                return "bin_number_3";
+            default:
+                return name;
         }
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/SeasonService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing AutoMapper.QueryableExtensions;\nusing eSusInsurers.Common.Exceptions;\nusing eSusInsurers.Constants;\nusing eSusInsurers.Domain.Entities;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Models.enums;\nusing eSusInsurers.Models.Extensions;\nusing eSusInsurers.Models.Helpers;\nusing eSusInsurers.Models.Seasons;\nusing eSusInsurers.Services.Common;\nusing Microsoft.EntityFrameworkCore;\nusing System.Linq.Dynamic.Core;\nusing System.Linq.Expressions;\n\nnamespace eSusInsurers.Services\n{\n    public class SeasonService(IUnitOfWork unitOfWork, IMapper mapper) : ISeasonService\n    {       \n        public async Task<List<SeasonModel>?> GetSeasonByYear(string year, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = SeasonsFilterByYear(year);\n            Expression<Func<Season, bool>> predicate = ExpressionBuilder<Season>.BuildFilterExpression(filters);\n\n            var query = unitOfWork.SeasonRepository.GetAll()\n               .Where(predicate)\n               .ProjectTo<SeasonModel>(mapper.ConfigurationProvider);\n\n            return await query.ToListAsync(cancellationToken);\n        }\n\n        public async Task<Models.Common.PagedResult<SeasonModel>> GetSeasons(GetSeasonQuery request, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = SeasonsFilters(request);\n            Expression<Func<Season, bool>> predicate = ExpressionBuilder<Season>.BuildFilterExpression(filters);\n            Dictionary<string, Models.Common.Filter> paginationFilters = FilterHelper.CreatePaginationFilters(request.pagingOptions);\n            (int PageSize, int Page) paginationParams = FilterHelper.GetPaginationParams(paginationFilters);\n            Dictionary<string, Models.Common.Filter> orderByFilters = FilterHelper.CreateOrderByFilters(request.sortingOptions);\n            var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);\n\n            var query = unitOfWork.SeasonRepository.GetAll()\n               .Where(predicate)\n               .ProjectTo<SeasonModel>(mapper.ConfigurationProvider);\n\n            if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) && orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == \"descending\")\n            {\n                orderByParams += ApplicationConstants.descending;\n            }\n\n            if (!string.IsNullOrEmpty(orderByParams))\n            {\n                query = query.OrderBy(orderByParams);\n            }\n\n            var seasons = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);\n\n            return new Models.Common.PagedResult<SeasonModel>\n            {\n                PageSize = paginationParams.PageSize,\n                TotalPages = seasons.TotalPages,\n                TotalRecordCount = seasons.TotalRecordCount,\n                Records = seasons.Records,\n                CurrentPage = paginationParams.Page\n            };\n\n        }\n\n        public async Task<int> AddSeason(SeasonRequest request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n\n            var season = await unitOfWork.SeasonRepository.GetBySeasonNameAsync(request.SeasonName, request.SeasonYear, cancellationToken);\n\n            if (season != null)\n                throw new BadRequestException($\"Season name: ({request.SeasonName}) already exists.\");\n\n            season = mapper.Map<Season>(request);\n\n            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                var entity = await unitOfWork.SeasonRepository.AddAsync(season, cancellationToken);\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync(cancellationToken);\n\n                return entity.Id;\n            }\n            catch (Exception ex)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n        }\n\n        public async Task UpdateSeason(int seasonId, UpdateSeasonRequest request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n\n            ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));\n\n            var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);\n\n            if (season == null)\n                throw new NotFoundException(\"Season Id doesn't exist.\");\n\n            var roleName = await unitOfWork.SeasonRepository.GetBySeasonNameAsync(seasonId, request.SeasonName, request.SeasonYear, cancellationToken);\n\n            if (roleName != null)\n                throw new BadRequestException($\"Season name: ({request.SeasonName}) and Season year: ({request.SeasonYear}) already exists.\");\n\n            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                mapper.Map(request, season);\n\n                await unitOfWork.SeasonRepository.UpdateAsync(season, cancellationToken);\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync(cancellationToken);\n            }\n            catch (Exception ex)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n                throw;\n            }\n        }\n\n        public async Task<bool> DeleteSeason(int seasonId, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));\n\n            var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);\n\n            if (season == null)\n                throw new NotFoundException($\"Season Id: ({seasonId}) doesn't exist.\");\n\n            season.IsActive = false;\n\n            await unitOfWork.SaveChangesAsync(cancellationToken);\n\n            return true;\n        }\n\n        public async Task<bool> ActivateSeason(int seasonId, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));\n\n            var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);\n\n            if (season == null)\n                throw new NotFoundException($\"Season Id: ({seasonId}) doesn't exist.\");\n\n            season.IsActive = true;\n\n            await unitOfWork.SaveChangesAsync(cancellationToken);\n\n            return true;\n        }\n\n        #region Private Methods\n        private static Dictionary<string, Models.Common.Filter> SeasonsFilters(GetSeasonQuery invQuery)\n        {\n            var inboundDto = invQuery.filter;\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            if (inboundDto != null)\n            {\n                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Year != null && inboundDto.Year.Count() > 0) ? string.Join(\",\", inboundDto.Year) : \"\", \"SeasonYear\", SearchOperationEnum.LContains);\n\n                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Season != null && inboundDto.Season.Count() > 0) ? string.Join(\",\", inboundDto.Season) : \"\", \"SeasonName\", SearchOperationEnum.LContains);\n\n                if (inboundDto?.IsActive != null)\n                    Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? \"True\" : \"False\", \"IsActive\", SearchOperationEnum.Equal);\n\n            }\n\n            return filters;\n        }\n\n        private static Dictionary<string, Models.Common.Filter> SeasonsFilterByYear(string year)\n        {\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            Filters.AddFilterIfNotEmpty(filters, \"True\", \"IsActive\", SearchOperationEnum.Equal);\n\n            Filters.AddFilterIfNotEmpty(filters, (year != null && year.Count() > 0) ? string.Join(\",\", year) : \"\", \"SeasonYear\", SearchOperationEnum.LContains);\n\n            return filters;\n        }     \n        #endregion\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/SeasonService.cs b/eSusInsurers/Services/Implementations/SeasonService.cs
--- a/eSusInsurers/Services/Implementations/SeasonService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/SeasonService.cs	(date 1732264029125)
@@ -1,3 +1,5 @@
+using System.Linq.Dynamic.Core;
+using System.Linq.Expressions;
 using AutoMapper;
 using AutoMapper.QueryableExtensions;
 using eSusInsurers.Common.Exceptions;
@@ -10,189 +12,195 @@
 using eSusInsurers.Models.Seasons;
 using eSusInsurers.Services.Common;
 using Microsoft.EntityFrameworkCore;
-using System.Linq.Dynamic.Core;
-using System.Linq.Expressions;
 
-namespace eSusInsurers.Services
-{
-    public class SeasonService(IUnitOfWork unitOfWork, IMapper mapper) : ISeasonService
-    {       
-        public async Task<List<SeasonModel>?> GetSeasonByYear(string year, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = SeasonsFilterByYear(year);
-            Expression<Func<Season, bool>> predicate = ExpressionBuilder<Season>.BuildFilterExpression(filters);
+namespace eSusInsurers.Services;
+
+public class SeasonService(IUnitOfWork unitOfWork, IMapper mapper) : ISeasonService
+{
+    public async Task<List<SeasonModel>?> GetSeasonByYear(string year, CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = SeasonsFilterByYear(year);
+        Expression<Func<Season, bool>> predicate = ExpressionBuilder<Season>.BuildFilterExpression(filters);
 
-            var query = unitOfWork.SeasonRepository.GetAll()
-               .Where(predicate)
-               .ProjectTo<SeasonModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.SeasonRepository.GetAll()
+            .Where(predicate)
+            .ProjectTo<SeasonModel>(mapper.ConfigurationProvider);
 
-            return await query.ToListAsync(cancellationToken);
-        }
+        return await query.ToListAsync(cancellationToken);
+    }
 
-        public async Task<Models.Common.PagedResult<SeasonModel>> GetSeasons(GetSeasonQuery request, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = SeasonsFilters(request);
-            Expression<Func<Season, bool>> predicate = ExpressionBuilder<Season>.BuildFilterExpression(filters);
-            Dictionary<string, Models.Common.Filter> paginationFilters = FilterHelper.CreatePaginationFilters(request.pagingOptions);
-            (int PageSize, int Page) paginationParams = FilterHelper.GetPaginationParams(paginationFilters);
-            Dictionary<string, Models.Common.Filter> orderByFilters = FilterHelper.CreateOrderByFilters(request.sortingOptions);
-            var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);
+    public async Task<Models.Common.PagedResult<SeasonModel>> GetSeasons(GetSeasonQuery request,
+        CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = SeasonsFilters(request);
+        Expression<Func<Season, bool>> predicate = ExpressionBuilder<Season>.BuildFilterExpression(filters);
+        Dictionary<string, Models.Common.Filter> paginationFilters =
+            FilterHelper.CreatePaginationFilters(request.pagingOptions);
+        var paginationParams = FilterHelper.GetPaginationParams(paginationFilters);
+        Dictionary<string, Models.Common.Filter> orderByFilters =
+            FilterHelper.CreateOrderByFilters(request.sortingOptions);
+        var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);
 
-            var query = unitOfWork.SeasonRepository.GetAll()
-               .Where(predicate)
-               .ProjectTo<SeasonModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.SeasonRepository.GetAll()
+            .Where(predicate)
+            .ProjectTo<SeasonModel>(mapper.ConfigurationProvider);
 
-            if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) && orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == "descending")
-            {
-                orderByParams += ApplicationConstants.descending;
-            }
+        if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) &&
+            orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == "descending")
+            orderByParams += ApplicationConstants.descending;
 
-            if (!string.IsNullOrEmpty(orderByParams))
-            {
-                query = query.OrderBy(orderByParams);
-            }
+        if (!string.IsNullOrEmpty(orderByParams)) query = query.OrderBy(orderByParams);
 
-            var seasons = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);
+        var seasons = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);
 
-            return new Models.Common.PagedResult<SeasonModel>
-            {
-                PageSize = paginationParams.PageSize,
-                TotalPages = seasons.TotalPages,
-                TotalRecordCount = seasons.TotalRecordCount,
-                Records = seasons.Records,
-                CurrentPage = paginationParams.Page
-            };
-
-        }
+        return new Models.Common.PagedResult<SeasonModel>
+        {
+            PageSize = paginationParams.PageSize,
+            TotalPages = seasons.TotalPages,
+            TotalRecordCount = seasons.TotalRecordCount,
+            Records = seasons.Records,
+            CurrentPage = paginationParams.Page
+        };
+    }
 
-        public async Task<int> AddSeason(SeasonRequest request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
+    public async Task<int> AddSeason(SeasonRequest request, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
 
-            var season = await unitOfWork.SeasonRepository.GetBySeasonNameAsync(request.SeasonName, request.SeasonYear, cancellationToken);
+        var season =
+            await unitOfWork.SeasonRepository.GetBySeasonNameAsync(request.SeasonName, request.SeasonYear,
+                cancellationToken);
 
-            if (season != null)
-                throw new BadRequestException($"Season name: ({request.SeasonName}) already exists.");
+        if (season != null)
+            throw new BadRequestException($"Season name: ({request.SeasonName}) already exists.");
 
-            season = mapper.Map<Season>(request);
+        season = mapper.Map<Season>(request);
 
-            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                var entity = await unitOfWork.SeasonRepository.AddAsync(season, cancellationToken);
+        try
+        {
+            var entity = await unitOfWork.SeasonRepository.AddAsync(season, cancellationToken);
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync(cancellationToken);
+            await transaction.CommitAsync(cancellationToken);
 
-                return entity.Id;
-            }
-            catch (Exception ex)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            return entity.Id;
+        }
+        catch (Exception ex)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
-        }
+            throw;
+        }
+    }
 
-        public async Task UpdateSeason(int seasonId, UpdateSeasonRequest request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
+    public async Task UpdateSeason(int seasonId, UpdateSeasonRequest request, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
 
-            ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));
+        ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));
 
-            var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);
+        var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);
 
-            if (season == null)
-                throw new NotFoundException("Season Id doesn't exist.");
+        if (season == null)
+            throw new NotFoundException("Season Id doesn't exist.");
 
-            var roleName = await unitOfWork.SeasonRepository.GetBySeasonNameAsync(seasonId, request.SeasonName, request.SeasonYear, cancellationToken);
+        var roleName = await unitOfWork.SeasonRepository.GetBySeasonNameAsync(seasonId, request.SeasonName,
+            request.SeasonYear, cancellationToken);
 
-            if (roleName != null)
-                throw new BadRequestException($"Season name: ({request.SeasonName}) and Season year: ({request.SeasonYear}) already exists.");
+        if (roleName != null)
+            throw new BadRequestException(
+                $"Season name: ({request.SeasonName}) and Season year: ({request.SeasonYear}) already exists.");
 
-            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                mapper.Map(request, season);
+        try
+        {
+            mapper.Map(request, season);
 
-                await unitOfWork.SeasonRepository.UpdateAsync(season, cancellationToken);
+            await unitOfWork.SeasonRepository.UpdateAsync(season, cancellationToken);
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync(cancellationToken);
-            }
-            catch (Exception ex)
-            {
-                await transaction.RollbackAsync(cancellationToken);
-                throw;
-            }
-        }
+            await transaction.CommitAsync(cancellationToken);
+        }
+        catch (Exception ex)
+        {
+            await transaction.RollbackAsync(cancellationToken);
+            throw;
+        }
+    }
 
-        public async Task<bool> DeleteSeason(int seasonId, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));
+    public async Task<bool> DeleteSeason(int seasonId, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));
 
-            var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);
+        var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);
 
-            if (season == null)
-                throw new NotFoundException($"Season Id: ({seasonId}) doesn't exist.");
+        if (season == null)
+            throw new NotFoundException($"Season Id: ({seasonId}) doesn't exist.");
 
-            season.IsActive = false;
+        season.IsActive = false;
 
-            await unitOfWork.SaveChangesAsync(cancellationToken);
+        await unitOfWork.SaveChangesAsync(cancellationToken);
 
-            return true;
-        }
+        return true;
+    }
 
-        public async Task<bool> ActivateSeason(int seasonId, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));
+    public async Task<bool> ActivateSeason(int seasonId, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(seasonId, nameof(seasonId));
 
-            var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);
+        var season = await unitOfWork.SeasonRepository.GetByIdAsync(seasonId, null, false, cancellationToken);
 
-            if (season == null)
-                throw new NotFoundException($"Season Id: ({seasonId}) doesn't exist.");
+        if (season == null)
+            throw new NotFoundException($"Season Id: ({seasonId}) doesn't exist.");
 
-            season.IsActive = true;
+        season.IsActive = true;
 
-            await unitOfWork.SaveChangesAsync(cancellationToken);
+        await unitOfWork.SaveChangesAsync(cancellationToken);
 
-            return true;
-        }
+        return true;
+    }
 
-        #region Private Methods
-        private static Dictionary<string, Models.Common.Filter> SeasonsFilters(GetSeasonQuery invQuery)
-        {
-            var inboundDto = invQuery.filter;
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    #region Private Methods
+
+    private static Dictionary<string, Models.Common.Filter> SeasonsFilters(GetSeasonQuery invQuery)
+    {
+        var inboundDto = invQuery.filter;
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            if (inboundDto != null)
-            {
-                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Year != null && inboundDto.Year.Count() > 0) ? string.Join(",", inboundDto.Year) : "", "SeasonYear", SearchOperationEnum.LContains);
+        if (inboundDto != null)
+        {
+            Filters.AddFilterIfNotEmpty(filters,
+                inboundDto?.Year != null && inboundDto.Year.Count() > 0 ? string.Join(",", inboundDto.Year) : "",
+                "SeasonYear", SearchOperationEnum.LContains);
 
-                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Season != null && inboundDto.Season.Count() > 0) ? string.Join(",", inboundDto.Season) : "", "SeasonName", SearchOperationEnum.LContains);
+            Filters.AddFilterIfNotEmpty(filters,
+                inboundDto?.Season != null && inboundDto.Season.Count() > 0 ? string.Join(",", inboundDto.Season) : "",
+                "SeasonName", SearchOperationEnum.LContains);
 
-                if (inboundDto?.IsActive != null)
-                    Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? "True" : "False", "IsActive", SearchOperationEnum.Equal);
-
-            }
+            if (inboundDto?.IsActive != null)
+                Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? "True" : "False", "IsActive",
+                    SearchOperationEnum.Equal);
+        }
 
-            return filters;
-        }
+        return filters;
+    }
 
-        private static Dictionary<string, Models.Common.Filter> SeasonsFilterByYear(string year)
-        {
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    private static Dictionary<string, Models.Common.Filter> SeasonsFilterByYear(string year)
+    {
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            Filters.AddFilterIfNotEmpty(filters, "True", "IsActive", SearchOperationEnum.Equal);
+        Filters.AddFilterIfNotEmpty(filters, "True", "IsActive", SearchOperationEnum.Equal);
 
-            Filters.AddFilterIfNotEmpty(filters, (year != null && year.Count() > 0) ? string.Join(",", year) : "", "SeasonYear", SearchOperationEnum.LContains);
+        Filters.AddFilterIfNotEmpty(filters, year != null && year.Count() > 0 ? string.Join(",", year) : "",
+            "SeasonYear", SearchOperationEnum.LContains);
 
-            return filters;
-        }     
-        #endregion
-    }
-}
+        return filters;
+    }
+
+    #endregion
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/EmailService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using EmailService.Interfaces;\nusing EmailService.Models;\nusing eSusInsurers.Helpers;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Models.Common;\nusing eSusInsurers.Services.Interfaces;\n\nnamespace eSusInsurers.Services.Implementations\n{\n    public class EmailService : IEmailService\n    {\n        private readonly IUpdateNotificationTemplate _updateNotificationTemplate;\n        private readonly IEmailSender _emailSender;\n        private readonly IUnitOfWork _unitOfWork;\n        private readonly FireForget _fireForget;\n\n        public EmailService(IUpdateNotificationTemplate updateNotificationTemplate\n                          , IEmailSender emailSender\n                          , IUnitOfWork unitOfWork\n                          , FireForget fireForget)\n        {\n            _updateNotificationTemplate = updateNotificationTemplate;\n            _emailSender = emailSender;\n            _unitOfWork = unitOfWork;\n            _fireForget = fireForget;\n        }\n\n        public async Task SendEmailAsync(NotificationContentParameters parameters\n                                                     , string eventName\n                                                     , string[] ToAddress\n                                                     , IFormFileCollection? attachments = null\n                                                     , CancellationToken cancellationToken = default)\n        {\n            _fireForget.Execute<IUnitOfWork>(async unitOfWork =>\n            {\n                try\n                {\n                    var templateDetails = await unitOfWork.EmailTemplateRepository.GetByEventNameAsync(eventName, cancellationToken);\n\n                    if (templateDetails != null)\n                    {\n                        var subject = _updateNotificationTemplate.UpdateNotificationContentParametrs(parameters, templateDetails.MailSubject, eventName);\n                        var content = _updateNotificationTemplate.UpdateNotificationContentParametrs(parameters, templateDetails.MailContent, eventName);\n                        var message = new Message(ToAddress, subject, content, attachments);\n                        await _emailSender.SendEmailAsync(message);\n                    }\n                }\n                catch (Exception ex)\n                {\n                    throw;\n                }\n            });\n        }\n    }\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/EmailService.cs b/eSusInsurers/Services/Implementations/EmailService.cs
--- a/eSusInsurers/Services/Implementations/EmailService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/EmailService.cs	(date 1732264029131)
@@ -5,51 +5,48 @@
 using eSusInsurers.Models.Common;
 using eSusInsurers.Services.Interfaces;
 
-namespace eSusInsurers.Services.Implementations
-{
-    public class EmailService : IEmailService
-    {
-        private readonly IUpdateNotificationTemplate _updateNotificationTemplate;
-        private readonly IEmailSender _emailSender;
-        private readonly IUnitOfWork _unitOfWork;
-        private readonly FireForget _fireForget;
+namespace eSusInsurers.Services.Implementations;
+
+public class EmailService : IEmailService
+{
+    private readonly IEmailSender _emailSender;
+    private readonly FireForget _fireForget;
+    private readonly IUnitOfWork _unitOfWork;
+    private readonly IUpdateNotificationTemplate _updateNotificationTemplate;
 
-        public EmailService(IUpdateNotificationTemplate updateNotificationTemplate
-                          , IEmailSender emailSender
-                          , IUnitOfWork unitOfWork
-                          , FireForget fireForget)
-        {
-            _updateNotificationTemplate = updateNotificationTemplate;
-            _emailSender = emailSender;
-            _unitOfWork = unitOfWork;
-            _fireForget = fireForget;
-        }
+    public EmailService(IUpdateNotificationTemplate updateNotificationTemplate
+        , IEmailSender emailSender
+        , IUnitOfWork unitOfWork
+        , FireForget fireForget)
+    {
+        _updateNotificationTemplate = updateNotificationTemplate;
+        _emailSender = emailSender;
+        _unitOfWork = unitOfWork;
+        _fireForget = fireForget;
+    }
 
-        public async Task SendEmailAsync(NotificationContentParameters parameters
-                                                     , string eventName
-                                                     , string[] ToAddress
-                                                     , IFormFileCollection? attachments = null
-                                                     , CancellationToken cancellationToken = default)
-        {
-            _fireForget.Execute<IUnitOfWork>(async unitOfWork =>
-            {
-                try
-                {
-                    var templateDetails = await unitOfWork.EmailTemplateRepository.GetByEventNameAsync(eventName, cancellationToken);
+    public async Task SendEmailAsync(NotificationContentParameters parameters
+        , string eventName
+        , string[] ToAddress
+        , IFormFileCollection? attachments = null
+        , CancellationToken cancellationToken = default)
+    {
+        _fireForget.Execute<IUnitOfWork>(async unitOfWork =>
+        {
+            var templateDetails =
+                await unitOfWork.EmailTemplateRepository.GetByEventNameAsync(eventName, cancellationToken);
 
-                    if (templateDetails != null)
-                    {
-                        var subject = _updateNotificationTemplate.UpdateNotificationContentParametrs(parameters, templateDetails.MailSubject, eventName);
-                        var content = _updateNotificationTemplate.UpdateNotificationContentParametrs(parameters, templateDetails.MailContent, eventName);
-                        var message = new Message(ToAddress, subject, content, attachments);
-                        await _emailSender.SendEmailAsync(message);
-                    }
-                }
-                catch (Exception ex)
-                {
-                    throw;
-                }
-            });
-        }
+            if (templateDetails != null)
+            {
+                var subject =
+                    _updateNotificationTemplate.UpdateNotificationContentParametrs(parameters,
+                        templateDetails.MailSubject, eventName);
+                var content =
+                    _updateNotificationTemplate.UpdateNotificationContentParametrs(parameters,
+                        templateDetails.MailContent, eventName);
+                var message = new Message(ToAddress, subject, content, attachments);
+                await _emailSender.SendEmailAsync(message);
+            }
+        });
     }
 }
\ No newline at end of file
Index: eSusInsurers/Policy/SnakeCaseNamingPolicy.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using eSusInsurers.Extensions;\nusing System.Text.Json;\n\nnamespace eSusInsurers.Policy\n{\n    /// <summary>\n    /// Policy for converting Pascal case fieldnames into snake naming(snake_case) convention.\n    /// </summary>\n    public class SnakeCaseNamingPolicy : JsonNamingPolicy\n    {\n        /// <summary>\n        /// Convert Pascal naming using snake_case using snake casing convention.\n        /// </summary>\n        /// <param name=\"name\">input to convert</param>\n        /// <returns>snake_case format of input</returns>\n        public override string ConvertName(string name) => name.ToSnakeCase();\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Policy/SnakeCaseNamingPolicy.cs b/eSusInsurers/Policy/SnakeCaseNamingPolicy.cs
--- a/eSusInsurers/Policy/SnakeCaseNamingPolicy.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Policy/SnakeCaseNamingPolicy.cs	(date 1732264029266)
@@ -1,18 +1,20 @@
-using eSusInsurers.Extensions;
 using System.Text.Json;
+using eSusInsurers.Extensions;
 
-namespace eSusInsurers.Policy
-{
-    /// <summary>
-    /// Policy for converting Pascal case fieldnames into snake naming(snake_case) convention.
-    /// </summary>
-    public class SnakeCaseNamingPolicy : JsonNamingPolicy
-    {
-        /// <summary>
-        /// Convert Pascal naming using snake_case using snake casing convention.
-        /// </summary>
-        /// <param name="name">input to convert</param>
-        /// <returns>snake_case format of input</returns>
-        public override string ConvertName(string name) => name.ToSnakeCase();
+namespace eSusInsurers.Policy;
+
+/// <summary>
+///     Policy for converting Pascal case fieldnames into snake naming(snake_case) convention.
+/// </summary>
+public class SnakeCaseNamingPolicy : JsonNamingPolicy
+{
+    /// <summary>
+    ///     Convert Pascal naming using snake_case using snake casing convention.
+    /// </summary>
+    /// <param name="name">input to convert</param>
+    /// <returns>snake_case format of input</returns>
+    public override string ConvertName(string name)
+    {
+        return name.ToSnakeCase();
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/CountriesService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Models.Countries;\nusing eSusInsurers.Models.enums;\nusing eSusInsurers.Models.Helpers;\nusing eSusInsurers.Services.Common;\nusing eSusInsurers.Services.Interfaces;\nusing System.Linq.Expressions;\nusing eSusInsurers.Domain.Entities;\nusing AutoMapper.QueryableExtensions;\nusing Microsoft.EntityFrameworkCore;\n\n\nnamespace eSusInsurers.Services.Implementations\n{\n    public class CountriesService(IUnitOfWork unitOfWork,\n                             IMapper mapper\n                            ) : ICountriesService\n    {\n        public async Task<List<RegionModel>?> GetRegions(long countryId, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = RegionFilterById(countryId);\n            Expression<Func<Region, bool>> predicate = ExpressionBuilder<Region>.BuildFilterExpression(filters);\n\n            var query = unitOfWork.CountriesRepository.GetAll()\n               .Where(predicate)\n               .ProjectTo<RegionModel>(mapper.ConfigurationProvider);\n\n            return  await query.ToListAsync(cancellationToken);\n        }\n\n        public async Task <List<DistrictModel>?> GetDistricts(long countryId, long regionId, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = DistrictFilterById(countryId, regionId);\n            Expression<Func<District, bool>> predicate = ExpressionBuilder<District>.BuildFilterExpression(filters);\n\n            var query = unitOfWork.DistrictRepository.GetAll(new string[] { \"Region\" })\n               .Where(predicate)\n               .ProjectTo<DistrictModel>(mapper.ConfigurationProvider);\n\n            return await query.ToListAsync(cancellationToken);\n        }\n\n        public async Task<List<SubCountiesModel>?> GetSubcounties(long countryId, long regionId, long districtId, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = SubcountyFilterById(countryId, regionId, districtId);\n            Expression<Func<SubCounty, bool>> predicate = ExpressionBuilder<SubCounty>.BuildFilterExpression(filters);\n\n            var query = unitOfWork.SubcountiesRepository\n                .GetAll(new string[] { \"District\", \"District.Region\" })\n               .Where(predicate)\n               .ProjectTo<SubCountiesModel>(mapper.ConfigurationProvider);\n\n            return await query.ToListAsync(cancellationToken);\n        }\n\n        public async Task<List<ParishModel>?> GetParishes(long countryId, long regionId, long districtId, long subCountyId, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = ParishFilterById(countryId, regionId, districtId, subCountyId);\n            Expression<Func<Parish, bool>> predicate = ExpressionBuilder<Parish>.BuildFilterExpression(filters);\n\n            var query = unitOfWork.ParishRepository\n                .GetAll(new string[] { \"SubCounty\", \"SubCounty.District\", \"SubCounty.District.Region\" })\n               .Where(predicate)\n               .ProjectTo<ParishModel>(mapper.ConfigurationProvider);\n\n            return await query.ToListAsync(cancellationToken);\n        }\n\n        private static Dictionary<string, Models.Common.Filter> RegionFilterById(long countryId)\n        {\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, countryId, \"CountryId\", SearchOperationEnum.Equal);\n\n            return filters;\n        }\n\n        private static Dictionary<string, Models.Common.Filter> DistrictFilterById(long countryId, long regionId)\n        {\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, countryId, \"Region.CountryId\", SearchOperationEnum.Equal);\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, regionId, \"RegionId\", SearchOperationEnum.Equal);\n\n            return filters;\n        }\n\n        private static Dictionary<string, Models.Common.Filter> SubcountyFilterById(long countryId, long regionId, long districtId)\n        {\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, countryId, \"District.Region.CountryId\", SearchOperationEnum.Equal);\n            \n            Filters.AddFilterIfValueGreaterThanZero(filters, regionId, \"District.RegionId\", SearchOperationEnum.Equal);\n            \n            Filters.AddFilterIfValueGreaterThanZero(filters, districtId, \"DistrictId\", SearchOperationEnum.Equal);\n\n            return filters;\n        }\n\n        private static Dictionary<string, Models.Common.Filter> ParishFilterById(long countryId, long regionId, long districtId, long subCountyId)\n        {\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, countryId, \"SubCounty.District.Region.CountryId\", SearchOperationEnum.Equal);\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, regionId, \"SubCounty.District.RegionId\", SearchOperationEnum.Equal);\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, districtId, \"SubCounty.DistrictId\", SearchOperationEnum.Equal);\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, subCountyId, \"SubCountyId\", SearchOperationEnum.Equal);\n\n            return filters;\n        }\n\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/CountriesService.cs b/eSusInsurers/Services/Implementations/CountriesService.cs
--- a/eSusInsurers/Services/Implementations/CountriesService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/CountriesService.cs	(date 1732264029121)
@@ -1,119 +1,126 @@
+using System.Linq.Expressions;
 using AutoMapper;
+using AutoMapper.QueryableExtensions;
+using eSusInsurers.Domain.Entities;
 using eSusInsurers.Infrastructure.Common;
 using eSusInsurers.Models.Countries;
 using eSusInsurers.Models.enums;
 using eSusInsurers.Models.Helpers;
 using eSusInsurers.Services.Common;
 using eSusInsurers.Services.Interfaces;
-using System.Linq.Expressions;
-using eSusInsurers.Domain.Entities;
-using AutoMapper.QueryableExtensions;
 using Microsoft.EntityFrameworkCore;
 
+namespace eSusInsurers.Services.Implementations;
 
-namespace eSusInsurers.Services.Implementations
-{
-    public class CountriesService(IUnitOfWork unitOfWork,
-                             IMapper mapper
-                            ) : ICountriesService
-    {
-        public async Task<List<RegionModel>?> GetRegions(long countryId, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = RegionFilterById(countryId);
-            Expression<Func<Region, bool>> predicate = ExpressionBuilder<Region>.BuildFilterExpression(filters);
+public class CountriesService(
+    IUnitOfWork unitOfWork,
+    IMapper mapper
+) : ICountriesService
+{
+    public async Task<List<RegionModel>?> GetRegions(long countryId, CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = RegionFilterById(countryId);
+        Expression<Func<Region, bool>> predicate = ExpressionBuilder<Region>.BuildFilterExpression(filters);
 
-            var query = unitOfWork.CountriesRepository.GetAll()
-               .Where(predicate)
-               .ProjectTo<RegionModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.CountriesRepository.GetAll()
+            .Where(predicate)
+            .ProjectTo<RegionModel>(mapper.ConfigurationProvider);
 
-            return  await query.ToListAsync(cancellationToken);
-        }
+        return await query.ToListAsync(cancellationToken);
+    }
 
-        public async Task <List<DistrictModel>?> GetDistricts(long countryId, long regionId, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = DistrictFilterById(countryId, regionId);
-            Expression<Func<District, bool>> predicate = ExpressionBuilder<District>.BuildFilterExpression(filters);
+    public async Task<List<DistrictModel>?> GetDistricts(long countryId, long regionId,
+        CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = DistrictFilterById(countryId, regionId);
+        Expression<Func<District, bool>> predicate = ExpressionBuilder<District>.BuildFilterExpression(filters);
 
-            var query = unitOfWork.DistrictRepository.GetAll(new string[] { "Region" })
-               .Where(predicate)
-               .ProjectTo<DistrictModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.DistrictRepository.GetAll(new[] { "Region" })
+            .Where(predicate)
+            .ProjectTo<DistrictModel>(mapper.ConfigurationProvider);
 
-            return await query.ToListAsync(cancellationToken);
-        }
+        return await query.ToListAsync(cancellationToken);
+    }
 
-        public async Task<List<SubCountiesModel>?> GetSubcounties(long countryId, long regionId, long districtId, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = SubcountyFilterById(countryId, regionId, districtId);
-            Expression<Func<SubCounty, bool>> predicate = ExpressionBuilder<SubCounty>.BuildFilterExpression(filters);
+    public async Task<List<SubCountiesModel>?> GetSubcounties(long countryId, long regionId, long districtId,
+        CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = SubcountyFilterById(countryId, regionId, districtId);
+        Expression<Func<SubCounty, bool>> predicate = ExpressionBuilder<SubCounty>.BuildFilterExpression(filters);
 
-            var query = unitOfWork.SubcountiesRepository
-                .GetAll(new string[] { "District", "District.Region" })
-               .Where(predicate)
-               .ProjectTo<SubCountiesModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.SubcountiesRepository
+            .GetAll(new[] { "District", "District.Region" })
+            .Where(predicate)
+            .ProjectTo<SubCountiesModel>(mapper.ConfigurationProvider);
 
-            return await query.ToListAsync(cancellationToken);
-        }
+        return await query.ToListAsync(cancellationToken);
+    }
 
-        public async Task<List<ParishModel>?> GetParishes(long countryId, long regionId, long districtId, long subCountyId, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = ParishFilterById(countryId, regionId, districtId, subCountyId);
-            Expression<Func<Parish, bool>> predicate = ExpressionBuilder<Parish>.BuildFilterExpression(filters);
+    public async Task<List<ParishModel>?> GetParishes(long countryId, long regionId, long districtId, long subCountyId,
+        CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters =
+            ParishFilterById(countryId, regionId, districtId, subCountyId);
+        Expression<Func<Parish, bool>> predicate = ExpressionBuilder<Parish>.BuildFilterExpression(filters);
 
-            var query = unitOfWork.ParishRepository
-                .GetAll(new string[] { "SubCounty", "SubCounty.District", "SubCounty.District.Region" })
-               .Where(predicate)
-               .ProjectTo<ParishModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.ParishRepository
+            .GetAll(new[] { "SubCounty", "SubCounty.District", "SubCounty.District.Region" })
+            .Where(predicate)
+            .ProjectTo<ParishModel>(mapper.ConfigurationProvider);
 
-            return await query.ToListAsync(cancellationToken);
-        }
+        return await query.ToListAsync(cancellationToken);
+    }
 
-        private static Dictionary<string, Models.Common.Filter> RegionFilterById(long countryId)
-        {
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    private static Dictionary<string, Models.Common.Filter> RegionFilterById(long countryId)
+    {
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, countryId, "CountryId", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, countryId, "CountryId", SearchOperationEnum.Equal);
 
-            return filters;
-        }
+        return filters;
+    }
 
-        private static Dictionary<string, Models.Common.Filter> DistrictFilterById(long countryId, long regionId)
-        {
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    private static Dictionary<string, Models.Common.Filter> DistrictFilterById(long countryId, long regionId)
+    {
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, countryId, "Region.CountryId", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, countryId, "Region.CountryId", SearchOperationEnum.Equal);
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, regionId, "RegionId", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, regionId, "RegionId", SearchOperationEnum.Equal);
 
-            return filters;
-        }
+        return filters;
+    }
 
-        private static Dictionary<string, Models.Common.Filter> SubcountyFilterById(long countryId, long regionId, long districtId)
-        {
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    private static Dictionary<string, Models.Common.Filter> SubcountyFilterById(long countryId, long regionId,
+        long districtId)
+    {
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, countryId, "District.Region.CountryId", SearchOperationEnum.Equal);
-            
-            Filters.AddFilterIfValueGreaterThanZero(filters, regionId, "District.RegionId", SearchOperationEnum.Equal);
-            
-            Filters.AddFilterIfValueGreaterThanZero(filters, districtId, "DistrictId", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, countryId, "District.Region.CountryId",
+            SearchOperationEnum.Equal);
+
+        Filters.AddFilterIfValueGreaterThanZero(filters, regionId, "District.RegionId", SearchOperationEnum.Equal);
+
+        Filters.AddFilterIfValueGreaterThanZero(filters, districtId, "DistrictId", SearchOperationEnum.Equal);
 
-            return filters;
-        }
+        return filters;
+    }
 
-        private static Dictionary<string, Models.Common.Filter> ParishFilterById(long countryId, long regionId, long districtId, long subCountyId)
-        {
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    private static Dictionary<string, Models.Common.Filter> ParishFilterById(long countryId, long regionId,
+        long districtId, long subCountyId)
+    {
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, countryId, "SubCounty.District.Region.CountryId", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, countryId, "SubCounty.District.Region.CountryId",
+            SearchOperationEnum.Equal);
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, regionId, "SubCounty.District.RegionId", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, regionId, "SubCounty.District.RegionId",
+            SearchOperationEnum.Equal);
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, districtId, "SubCounty.DistrictId", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, districtId, "SubCounty.DistrictId", SearchOperationEnum.Equal);
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, subCountyId, "SubCountyId", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, subCountyId, "SubCountyId", SearchOperationEnum.Equal);
 
-            return filters;
-        }
-
+        return filters;
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Helpers/AppEvents.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>namespace eSusInsurers.Helpers\n{\n    public class AppEvents\n    {\n        public static string SendOtp = \"SendOTP\";\n        public static string CreateUser = \"CreateUser\";\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Helpers/AppEvents.cs b/eSusInsurers/Helpers/AppEvents.cs
--- a/eSusInsurers/Helpers/AppEvents.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Helpers/AppEvents.cs	(date 1732264029212)
@@ -1,8 +1,7 @@
-namespace eSusInsurers.Helpers
-{
-    public class AppEvents
-    {
-        public static string SendOtp = "SendOTP";
-        public static string CreateUser = "CreateUser";
-    }
-}
+namespace eSusInsurers.Helpers;
+
+public class AppEvents
+{
+    public static string SendOtp = "SendOTP";
+    public static string CreateUser = "CreateUser";
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/SeasonCutOffDateService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing AutoMapper.QueryableExtensions;\nusing eSusInsurers.Constants;\nusing eSusInsurers.Domain.Entities;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Models.Extensions;\nusing eSusInsurers.Models.Helpers;\nusing eSusInsurers.Models.SeasonCutOffDate;\nusing eSusInsurers.Services.Common;\nusing eSusInsurers.Services.Interfaces;\nusing System.Linq.Expressions;\nusing System.Linq.Dynamic.Core;\nusing eSusInsurers.Models.enums;\nusing eSusInsurers.Common.Exceptions;\nusing Microsoft.EntityFrameworkCore;\n\nnamespace eSusInsurers.Services.Implementations\n{\n    public class SeasonCutOffDateService(IUnitOfWork unitOfWork, IMapper mapper) : ISeasonCutOffDateService\n    {\n        public async Task<SeasonCutOffDatesModel?> GetSeasonCutOffDatesById(int seasonCutOffDateId, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = SeasonCutOffDatesFiltersById(seasonCutOffDateId);\n            Expression<Func<SeasonCutOffDate, bool>> predicate = ExpressionBuilder<SeasonCutOffDate>.BuildFilterExpression(filters);\n\n            var query = unitOfWork.SeasonCutOffDateRepository.GetAll()\n               .Where(predicate)\n               .ProjectTo<SeasonCutOffDatesModel>(mapper.ConfigurationProvider);\n\n            return await query.FirstOrDefaultAsync(cancellationToken);\n        }\n\n        public async Task<Models.Common.PagedResult<SeasonCutOffDatesModel>> GetSeasonCutOffDates(GetSeasonCutOffDatesQuery request, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = SeasonCutOffDatesFilters(request);\n            Expression<Func<SeasonCutOffDate, bool>> predicate = ExpressionBuilder<SeasonCutOffDate>.BuildFilterExpression(filters);\n            Dictionary<string, Models.Common.Filter> paginationFilters = FilterHelper.CreatePaginationFilters(request.pagingOptions);\n            (int PageSize, int Page) paginationParams = FilterHelper.GetPaginationParams(paginationFilters);\n            Dictionary<string, Models.Common.Filter> orderByFilters = FilterHelper.CreateOrderByFilters(request.sortingOptions);\n            var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);\n\n            var query = unitOfWork.SeasonCutOffDateRepository.GetAll(new string[]\n                   {\n                       \"Crop\",  \"CropCategory\",  \"Region\",  \"Season\"\n                   })\n               .Where(predicate)\n               .ProjectTo<SeasonCutOffDatesModel>(mapper.ConfigurationProvider);\n\n            if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) && orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == \"descending\")\n            {\n                orderByParams += ApplicationConstants.descending;\n            }\n\n            if (!string.IsNullOrEmpty(orderByParams))\n            {\n                query = query.OrderBy(orderByParams);\n            }\n\n            var seasons = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);\n\n            return new Models.Common.PagedResult<SeasonCutOffDatesModel>\n            {\n                PageSize = paginationParams.PageSize,\n                TotalPages = seasons.TotalPages,\n                TotalRecordCount = seasons.TotalRecordCount,\n                Records = seasons.Records,\n                CurrentPage = paginationParams.Page\n            };\n\n        }\n\n        public async Task<bool> AddSeasonCutOffDates(List<SeasonCutOffDatesRequest> request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n\n            foreach (var season in request)\n            {\n                var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(season.SeasonId, season.RegionId, season.CropCategoryId, season.CropId, cancellationToken);\n\n                if (seasonCutOffDates != null)\n                    throw new BadRequestException($\"Request already exists.\");\n            }\n\n            var seasonCutOffDate = mapper.Map<List<SeasonCutOffDate>>(request);\n\n            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                await unitOfWork.SeasonCutOffDateRepository.AddRangeAsync(seasonCutOffDate, cancellationToken);\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync(cancellationToken);\n\n                return true;\n            }\n            catch (Exception ex)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n        }\n\n        public async Task<bool> SeasonCutOffDatesExistenceCheck(SeasonCutOffDatesRequest request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(request.SeasonId, request.RegionId, request.CropCategoryId, request.CropId, cancellationToken);\n\n            if (seasonCutOffDates != null)\n                throw new BadRequestException($\"Request already exists.\");\n            return true;\n\n        }\n        public async Task UpdateSeasonCutOffDates(int seasonCutOffDateId, UpdateSeasonCutOffDatesRequest request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n            ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));\n            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false, cancellationToken);\n            if (seasonCutOffDates == null)\n                throw new NotFoundException(\"Season CutOff Date Id doesn't exist.\");\n            var seasonCutOffDate = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(seasonCutOffDateId, request.SeasonId, request.RegionId, request.CropCategoryId, request.CropId, cancellationToken);\n\n\n            if (seasonCutOffDate != null)\n                throw new BadRequestException($\"Request already exists.\");\n\n            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                mapper.Map(request, seasonCutOffDates);\n\n                await unitOfWork.SeasonCutOffDateRepository.UpdateAsync(seasonCutOffDates, cancellationToken);\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync(cancellationToken);\n            }\n            catch (Exception ex)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n                throw;\n            }\n        }\n\n        public async Task<bool> DeleteSeasonCutOffDates(int seasonCutOffDateId, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));\n\n            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false, cancellationToken);\n\n            if (seasonCutOffDates == null)\n                throw new NotFoundException($\"Season CutOff Date Id: ({seasonCutOffDateId}) doesn't exist.\");\n\n            seasonCutOffDates.IsActive = false;\n\n            await unitOfWork.SaveChangesAsync(cancellationToken);\n\n            return true;\n        }\n\n        public async Task<bool> ActivateSeasonCutOffDates(int seasonCutOffDateId, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));\n\n            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false, cancellationToken);\n\n            if (seasonCutOffDates == null)\n                throw new NotFoundException($\"Season CutOff Date Id: ({seasonCutOffDateId}) doesn't exist.\");\n\n            seasonCutOffDates.IsActive = true;\n\n            await unitOfWork.SaveChangesAsync(cancellationToken);\n\n            return true;\n        }\n\n        #region Private Methods\n        private static Dictionary<string, Models.Common.Filter> SeasonCutOffDatesFilters(GetSeasonCutOffDatesQuery invQuery)\n        {\n            var inboundDto = invQuery.filter;\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            if (inboundDto != null)\n            {\n                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Region != null && inboundDto.Region.Count() > 0) ? string.Join(\",\", inboundDto.Region) : \"\", \"Region.RegionName\", SearchOperationEnum.LContains);\n\n                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Crop != null && inboundDto.Crop.Count() > 0) ? string.Join(\",\", inboundDto.Crop) : \"\", \"Crop.CropName\", SearchOperationEnum.LContains);\n\n                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.CropCategory != null && inboundDto.CropCategory.Count() > 0) ? string.Join(\",\", inboundDto.CropCategory) : \"\", \"CropCategory.CropCategoryName\", SearchOperationEnum.LContains);\n\n                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Year != null && inboundDto.Year.Count() > 0) ? string.Join(\",\", inboundDto.Year) : \"\", \"Season.SeasonYear\", SearchOperationEnum.LContains);\n\n                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Season != null && inboundDto.Season.Count() > 0) ? string.Join(\",\", inboundDto.Season) : \"\", \"Season.SeasonName\", SearchOperationEnum.LContains);\n\n                if (inboundDto?.IsActive != null)\n                    Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? \"True\" : \"False\", \"IsActive\", SearchOperationEnum.Equal);\n\n            }\n\n            return filters;\n        }\n\n        private static Dictionary<string, Models.Common.Filter> SeasonCutOffDatesFiltersById(int seasonCutOffDateId)\n        {\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            Filters.AddFilterIfValueGreaterThanZero(filters, seasonCutOffDateId, \"Id\", SearchOperationEnum.Equal);\n\n            return filters;\n        }\n        #endregion\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/SeasonCutOffDateService.cs b/eSusInsurers/Services/Implementations/SeasonCutOffDateService.cs
--- a/eSusInsurers/Services/Implementations/SeasonCutOffDateService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/SeasonCutOffDateService.cs	(date 1732264029114)
@@ -1,216 +1,242 @@
+using System.Linq.Dynamic.Core;
+using System.Linq.Expressions;
 using AutoMapper;
 using AutoMapper.QueryableExtensions;
+using eSusInsurers.Common.Exceptions;
 using eSusInsurers.Constants;
 using eSusInsurers.Domain.Entities;
 using eSusInsurers.Infrastructure.Common;
+using eSusInsurers.Models.enums;
 using eSusInsurers.Models.Extensions;
 using eSusInsurers.Models.Helpers;
 using eSusInsurers.Models.SeasonCutOffDate;
 using eSusInsurers.Services.Common;
 using eSusInsurers.Services.Interfaces;
-using System.Linq.Expressions;
-using System.Linq.Dynamic.Core;
-using eSusInsurers.Models.enums;
-using eSusInsurers.Common.Exceptions;
 using Microsoft.EntityFrameworkCore;
 
-namespace eSusInsurers.Services.Implementations
-{
-    public class SeasonCutOffDateService(IUnitOfWork unitOfWork, IMapper mapper) : ISeasonCutOffDateService
-    {
-        public async Task<SeasonCutOffDatesModel?> GetSeasonCutOffDatesById(int seasonCutOffDateId, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = SeasonCutOffDatesFiltersById(seasonCutOffDateId);
-            Expression<Func<SeasonCutOffDate, bool>> predicate = ExpressionBuilder<SeasonCutOffDate>.BuildFilterExpression(filters);
+namespace eSusInsurers.Services.Implementations;
+
+public class SeasonCutOffDateService(IUnitOfWork unitOfWork, IMapper mapper) : ISeasonCutOffDateService
+{
+    public async Task<SeasonCutOffDatesModel?> GetSeasonCutOffDatesById(int seasonCutOffDateId,
+        CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = SeasonCutOffDatesFiltersById(seasonCutOffDateId);
+        Expression<Func<SeasonCutOffDate, bool>> predicate =
+            ExpressionBuilder<SeasonCutOffDate>.BuildFilterExpression(filters);
 
-            var query = unitOfWork.SeasonCutOffDateRepository.GetAll()
-               .Where(predicate)
-               .ProjectTo<SeasonCutOffDatesModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.SeasonCutOffDateRepository.GetAll()
+            .Where(predicate)
+            .ProjectTo<SeasonCutOffDatesModel>(mapper.ConfigurationProvider);
 
-            return await query.FirstOrDefaultAsync(cancellationToken);
-        }
+        return await query.FirstOrDefaultAsync(cancellationToken);
+    }
 
-        public async Task<Models.Common.PagedResult<SeasonCutOffDatesModel>> GetSeasonCutOffDates(GetSeasonCutOffDatesQuery request, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = SeasonCutOffDatesFilters(request);
-            Expression<Func<SeasonCutOffDate, bool>> predicate = ExpressionBuilder<SeasonCutOffDate>.BuildFilterExpression(filters);
-            Dictionary<string, Models.Common.Filter> paginationFilters = FilterHelper.CreatePaginationFilters(request.pagingOptions);
-            (int PageSize, int Page) paginationParams = FilterHelper.GetPaginationParams(paginationFilters);
-            Dictionary<string, Models.Common.Filter> orderByFilters = FilterHelper.CreateOrderByFilters(request.sortingOptions);
-            var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);
+    public async Task<Models.Common.PagedResult<SeasonCutOffDatesModel>> GetSeasonCutOffDates(
+        GetSeasonCutOffDatesQuery request, CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = SeasonCutOffDatesFilters(request);
+        Expression<Func<SeasonCutOffDate, bool>> predicate =
+            ExpressionBuilder<SeasonCutOffDate>.BuildFilterExpression(filters);
+        Dictionary<string, Models.Common.Filter> paginationFilters =
+            FilterHelper.CreatePaginationFilters(request.pagingOptions);
+        var paginationParams = FilterHelper.GetPaginationParams(paginationFilters);
+        Dictionary<string, Models.Common.Filter> orderByFilters =
+            FilterHelper.CreateOrderByFilters(request.sortingOptions);
+        var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);
 
-            var query = unitOfWork.SeasonCutOffDateRepository.GetAll(new string[]
-                   {
-                       "Crop",  "CropCategory",  "Region",  "Season"
-                   })
-               .Where(predicate)
-               .ProjectTo<SeasonCutOffDatesModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.SeasonCutOffDateRepository.GetAll(new[]
+            {
+                "Crop", "CropCategory", "Region", "Season"
+            })
+            .Where(predicate)
+            .ProjectTo<SeasonCutOffDatesModel>(mapper.ConfigurationProvider);
 
-            if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) && orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == "descending")
-            {
-                orderByParams += ApplicationConstants.descending;
-            }
+        if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) &&
+            orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == "descending")
+            orderByParams += ApplicationConstants.descending;
 
-            if (!string.IsNullOrEmpty(orderByParams))
-            {
-                query = query.OrderBy(orderByParams);
-            }
+        if (!string.IsNullOrEmpty(orderByParams)) query = query.OrderBy(orderByParams);
 
-            var seasons = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);
+        var seasons = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);
 
-            return new Models.Common.PagedResult<SeasonCutOffDatesModel>
-            {
-                PageSize = paginationParams.PageSize,
-                TotalPages = seasons.TotalPages,
-                TotalRecordCount = seasons.TotalRecordCount,
-                Records = seasons.Records,
-                CurrentPage = paginationParams.Page
-            };
-
-        }
+        return new Models.Common.PagedResult<SeasonCutOffDatesModel>
+        {
+            PageSize = paginationParams.PageSize,
+            TotalPages = seasons.TotalPages,
+            TotalRecordCount = seasons.TotalRecordCount,
+            Records = seasons.Records,
+            CurrentPage = paginationParams.Page
+        };
+    }
 
-        public async Task<bool> AddSeasonCutOffDates(List<SeasonCutOffDatesRequest> request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
+    public async Task<bool> AddSeasonCutOffDates(List<SeasonCutOffDatesRequest> request,
+        CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
 
-            foreach (var season in request)
-            {
-                var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(season.SeasonId, season.RegionId, season.CropCategoryId, season.CropId, cancellationToken);
+        foreach (var season in request)
+        {
+            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(
+                season.SeasonId, season.RegionId, season.CropCategoryId, season.CropId, cancellationToken);
 
-                if (seasonCutOffDates != null)
-                    throw new BadRequestException($"Request already exists.");
-            }
+            if (seasonCutOffDates != null)
+                throw new BadRequestException("Request already exists.");
+        }
 
-            var seasonCutOffDate = mapper.Map<List<SeasonCutOffDate>>(request);
+        var seasonCutOffDate = mapper.Map<List<SeasonCutOffDate>>(request);
 
-            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                await unitOfWork.SeasonCutOffDateRepository.AddRangeAsync(seasonCutOffDate, cancellationToken);
+        try
+        {
+            await unitOfWork.SeasonCutOffDateRepository.AddRangeAsync(seasonCutOffDate, cancellationToken);
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync(cancellationToken);
+            await transaction.CommitAsync(cancellationToken);
 
-                return true;
-            }
-            catch (Exception ex)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            return true;
+        }
+        catch (Exception ex)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
-        }
+            throw;
+        }
+    }
 
-        public async Task<bool> SeasonCutOffDatesExistenceCheck(SeasonCutOffDatesRequest request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
-            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(request.SeasonId, request.RegionId, request.CropCategoryId, request.CropId, cancellationToken);
+    public async Task<bool> SeasonCutOffDatesExistenceCheck(SeasonCutOffDatesRequest request,
+        CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
+        var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(request.SeasonId,
+            request.RegionId, request.CropCategoryId, request.CropId, cancellationToken);
 
-            if (seasonCutOffDates != null)
-                throw new BadRequestException($"Request already exists.");
-            return true;
-
-        }
-        public async Task UpdateSeasonCutOffDates(int seasonCutOffDateId, UpdateSeasonCutOffDatesRequest request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
-            ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));
-            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false, cancellationToken);
-            if (seasonCutOffDates == null)
-                throw new NotFoundException("Season CutOff Date Id doesn't exist.");
-            var seasonCutOffDate = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(seasonCutOffDateId, request.SeasonId, request.RegionId, request.CropCategoryId, request.CropId, cancellationToken);
+        if (seasonCutOffDates != null)
+            throw new BadRequestException("Request already exists.");
+        return true;
+    }
+
+    public async Task UpdateSeasonCutOffDates(int seasonCutOffDateId, UpdateSeasonCutOffDatesRequest request,
+        CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
+        ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));
+        var seasonCutOffDates =
+            await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false,
+                cancellationToken);
+        if (seasonCutOffDates == null)
+            throw new NotFoundException("Season CutOff Date Id doesn't exist.");
+        var seasonCutOffDate = await unitOfWork.SeasonCutOffDateRepository.GetBySeasonCutOffDateAsync(
+            seasonCutOffDateId, request.SeasonId, request.RegionId, request.CropCategoryId, request.CropId,
+            cancellationToken);
 
 
-            if (seasonCutOffDate != null)
-                throw new BadRequestException($"Request already exists.");
+        if (seasonCutOffDate != null)
+            throw new BadRequestException("Request already exists.");
 
-            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                mapper.Map(request, seasonCutOffDates);
+        try
+        {
+            mapper.Map(request, seasonCutOffDates);
 
-                await unitOfWork.SeasonCutOffDateRepository.UpdateAsync(seasonCutOffDates, cancellationToken);
+            await unitOfWork.SeasonCutOffDateRepository.UpdateAsync(seasonCutOffDates, cancellationToken);
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync(cancellationToken);
-            }
-            catch (Exception ex)
-            {
-                await transaction.RollbackAsync(cancellationToken);
-                throw;
-            }
-        }
+            await transaction.CommitAsync(cancellationToken);
+        }
+        catch (Exception ex)
+        {
+            await transaction.RollbackAsync(cancellationToken);
+            throw;
+        }
+    }
 
-        public async Task<bool> DeleteSeasonCutOffDates(int seasonCutOffDateId, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));
+    public async Task<bool> DeleteSeasonCutOffDates(int seasonCutOffDateId, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));
 
-            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false, cancellationToken);
+        var seasonCutOffDates =
+            await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false,
+                cancellationToken);
 
-            if (seasonCutOffDates == null)
-                throw new NotFoundException($"Season CutOff Date Id: ({seasonCutOffDateId}) doesn't exist.");
+        if (seasonCutOffDates == null)
+            throw new NotFoundException($"Season CutOff Date Id: ({seasonCutOffDateId}) doesn't exist.");
 
-            seasonCutOffDates.IsActive = false;
+        seasonCutOffDates.IsActive = false;
 
-            await unitOfWork.SaveChangesAsync(cancellationToken);
+        await unitOfWork.SaveChangesAsync(cancellationToken);
 
-            return true;
-        }
+        return true;
+    }
 
-        public async Task<bool> ActivateSeasonCutOffDates(int seasonCutOffDateId, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));
+    public async Task<bool> ActivateSeasonCutOffDates(int seasonCutOffDateId, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(seasonCutOffDateId, nameof(seasonCutOffDateId));
 
-            var seasonCutOffDates = await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false, cancellationToken);
+        var seasonCutOffDates =
+            await unitOfWork.SeasonCutOffDateRepository.GetByIdAsync(seasonCutOffDateId, null, false,
+                cancellationToken);
 
-            if (seasonCutOffDates == null)
-                throw new NotFoundException($"Season CutOff Date Id: ({seasonCutOffDateId}) doesn't exist.");
+        if (seasonCutOffDates == null)
+            throw new NotFoundException($"Season CutOff Date Id: ({seasonCutOffDateId}) doesn't exist.");
 
-            seasonCutOffDates.IsActive = true;
+        seasonCutOffDates.IsActive = true;
 
-            await unitOfWork.SaveChangesAsync(cancellationToken);
+        await unitOfWork.SaveChangesAsync(cancellationToken);
 
-            return true;
-        }
+        return true;
+    }
 
-        #region Private Methods
-        private static Dictionary<string, Models.Common.Filter> SeasonCutOffDatesFilters(GetSeasonCutOffDatesQuery invQuery)
-        {
-            var inboundDto = invQuery.filter;
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    #region Private Methods
+
+    private static Dictionary<string, Models.Common.Filter> SeasonCutOffDatesFilters(GetSeasonCutOffDatesQuery invQuery)
+    {
+        var inboundDto = invQuery.filter;
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            if (inboundDto != null)
-            {
-                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Region != null && inboundDto.Region.Count() > 0) ? string.Join(",", inboundDto.Region) : "", "Region.RegionName", SearchOperationEnum.LContains);
+        if (inboundDto != null)
+        {
+            Filters.AddFilterIfNotEmpty(filters,
+                inboundDto?.Region != null && inboundDto.Region.Count() > 0 ? string.Join(",", inboundDto.Region) : "",
+                "Region.RegionName", SearchOperationEnum.LContains);
 
-                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Crop != null && inboundDto.Crop.Count() > 0) ? string.Join(",", inboundDto.Crop) : "", "Crop.CropName", SearchOperationEnum.LContains);
+            Filters.AddFilterIfNotEmpty(filters,
+                inboundDto?.Crop != null && inboundDto.Crop.Count() > 0 ? string.Join(",", inboundDto.Crop) : "",
+                "Crop.CropName", SearchOperationEnum.LContains);
 
-                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.CropCategory != null && inboundDto.CropCategory.Count() > 0) ? string.Join(",", inboundDto.CropCategory) : "", "CropCategory.CropCategoryName", SearchOperationEnum.LContains);
+            Filters.AddFilterIfNotEmpty(filters,
+                inboundDto?.CropCategory != null && inboundDto.CropCategory.Count() > 0
+                    ? string.Join(",", inboundDto.CropCategory)
+                    : "", "CropCategory.CropCategoryName", SearchOperationEnum.LContains);
 
-                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Year != null && inboundDto.Year.Count() > 0) ? string.Join(",", inboundDto.Year) : "", "Season.SeasonYear", SearchOperationEnum.LContains);
+            Filters.AddFilterIfNotEmpty(filters,
+                inboundDto?.Year != null && inboundDto.Year.Count() > 0 ? string.Join(",", inboundDto.Year) : "",
+                "Season.SeasonYear", SearchOperationEnum.LContains);
 
-                Filters.AddFilterIfNotEmpty(filters, (inboundDto?.Season != null && inboundDto.Season.Count() > 0) ? string.Join(",", inboundDto.Season) : "", "Season.SeasonName", SearchOperationEnum.LContains);
+            Filters.AddFilterIfNotEmpty(filters,
+                inboundDto?.Season != null && inboundDto.Season.Count() > 0 ? string.Join(",", inboundDto.Season) : "",
+                "Season.SeasonName", SearchOperationEnum.LContains);
 
-                if (inboundDto?.IsActive != null)
-                    Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? "True" : "False", "IsActive", SearchOperationEnum.Equal);
-
-            }
+            if (inboundDto?.IsActive != null)
+                Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? "True" : "False", "IsActive",
+                    SearchOperationEnum.Equal);
+        }
 
-            return filters;
-        }
+        return filters;
+    }
 
-        private static Dictionary<string, Models.Common.Filter> SeasonCutOffDatesFiltersById(int seasonCutOffDateId)
-        {
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    private static Dictionary<string, Models.Common.Filter> SeasonCutOffDatesFiltersById(int seasonCutOffDateId)
+    {
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            Filters.AddFilterIfValueGreaterThanZero(filters, seasonCutOffDateId, "Id", SearchOperationEnum.Equal);
+        Filters.AddFilterIfValueGreaterThanZero(filters, seasonCutOffDateId, "Id", SearchOperationEnum.Equal);
 
-            return filters;
-        }
-        #endregion
-    }
-}
+        return filters;
+    }
+
+    #endregion
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/ProgramService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing AutoMapper.QueryableExtensions;\nusing eSusInsurers.Common.Exceptions;\nusing eSusInsurers.Constants;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Infrastructure.Interfaces;\nusing eSusInsurers.Models.enums;\nusing eSusInsurers.Models.Extensions;\nusing eSusInsurers.Models.Helpers;\nusing eSusInsurers.Models.Programs;\nusing eSusInsurers.Services.Common;\nusing eSusInsurers.Services.Interfaces;\nusing Microsoft.EntityFrameworkCore;\nusing System.Linq.Dynamic.Core;\nusing System.Linq.Expressions;\n\nnamespace eSusInsurers.Services.Implementations\n{\n    public class ProgramService(IUnitOfWork unitOfWork,\n                             IMapper mapper) : IProgramsService\n    {\n\n        public async Task<Models.Common.PagedResult<ProgramsModel>> GetPrograms(GetProgramsQuery request, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = ProgarmsFilters(request);\n            Expression<Func<eSusInsurers.Domain.Entities.Program, bool>> predicate = ExpressionBuilder<eSusInsurers.Domain.Entities.Program>.BuildFilterExpression(filters);\n            Dictionary<string, Models.Common.Filter> paginationFilters = FilterHelper.CreatePaginationFilters(request.pagingOptions);\n            (int PageSize, int Page) paginationParams = FilterHelper.GetPaginationParams(paginationFilters);\n            Dictionary<string, Models.Common.Filter> orderByFilters = FilterHelper.CreateOrderByFilters(request.sortingOptions);\n            var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);\n\n            var query = unitOfWork.ProgramRepository.GetAll(\n                new string[]\n                   {\n                        \"Region\", \"District\", \"SubCounty\", \"Parish\"\n                   })\n               .Where(predicate)\n               .OrderByDescending(x => x.Id)\n               .ProjectTo<ProgramsModel>(mapper.ConfigurationProvider);\n\n            if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) && orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == \"descending\")\n            {\n                orderByParams += ApplicationConstants.descending;\n            }\n\n            if (!string.IsNullOrEmpty(orderByParams))\n            {\n                query = query.OrderBy(orderByParams);\n            }\n\n            var programs = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);\n\n            return new Models.Common.PagedResult<ProgramsModel>\n            {\n                PageSize = paginationParams.PageSize,\n                TotalPages = programs.TotalPages,\n                TotalRecordCount = programs.TotalRecordCount,\n                Records = programs.Records,\n                CurrentPage = paginationParams.Page\n            };\n\n        }\n\n        public async Task<bool> AddProgram(ProgramRequest request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n\n            var program = await unitOfWork.ProgramRepository.GetAll()\n                .Where(x => x.ProgramName == request.ProgramName)\n                .FirstOrDefaultAsync(cancellationToken);\n\n            if (program != null)\n                throw new Exception($\"Program Name ({request.ProgramName}) already exists.\");\n\n            program = mapper.Map<eSusInsurers.Domain.Entities.Program>(request);\n\n            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                await unitOfWork.ProgramRepository.AddAsync(program, cancellationToken);\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync(cancellationToken);\n\n            }\n            catch (Exception)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n\n            return true;\n        }\n\n        public async Task UpdateProgram(int programId, ProgramRequest request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n\n            ArgumentNullException.ThrowIfNull(programId, nameof(programId));\n\n            var programs = await unitOfWork.ProgramRepository.GetAll()\n                .Where(x => x.ProgramName == request.ProgramName)\n                .ToListAsync(cancellationToken);\n\n            if (programs.Any(x => x.Id != programId))\n                throw new BadRequestException(\"Program Name and Institution Name already exists.\");\n\n            var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);\n\n            if (program == null)\n                throw new NotFoundException(\"Program Id doesn't exist.\");\n\n            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                mapper.Map(request, program);\n\n                await unitOfWork.ProgramRepository.UpdateAsync(program, cancellationToken);\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync(cancellationToken);\n            }\n            catch (Exception)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n        }\n\n        public async Task DeleteProgram(int programId, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(programId, nameof(programId));\n\n            var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);\n\n            if (program == null)\n                throw new NotFoundException($\"Program Id: ({programId}) doesn't exist.\");\n\n            program.IsActive = false;\n\n            await unitOfWork.SaveChangesAsync(cancellationToken);\n        }\n\n        public async Task ActivateProgram(int programId, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(programId, nameof(programId));\n\n            var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);\n\n            if (program == null)\n                throw new NotFoundException($\"Program Id: ({programId}) doesn't exist.\");\n\n            program.IsActive = true;\n\n            await unitOfWork.SaveChangesAsync(cancellationToken);\n        }\n\n\n        private static Dictionary<string, Models.Common.Filter> ProgarmsFilters(GetProgramsQuery invQuery)\n        {\n            var inboundDto = invQuery.filter;\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            if (inboundDto != null)\n            {\n\n                Filters.AddFilterIfNotEmpty(filters, inboundDto?.ProgramName, \"ProgramName\", SearchOperationEnum.Contains);\n\n                Filters.AddFilterIfNotEmpty(filters, inboundDto?.RegionName, \"Region.RegionName\", SearchOperationEnum.Contains);\n\n                Filters.AddFilterIfNotEmpty(filters, inboundDto?.DistrictName, \"District.DistrictName\", SearchOperationEnum.Contains);\n\n                Filters.AddFilterIfNotEmpty(filters, inboundDto?.SubCountyName, \"SubCounty.SubCountyName\", SearchOperationEnum.Contains);\n\n                Filters.AddFilterIfNotEmpty(filters, inboundDto?.ParishName, \"Parish.ParishName\", SearchOperationEnum.Contains);\n\n                Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.RegionId, \"RegionId\", SearchOperationEnum.Equal);\n\n                Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.DistrictId, \"DistrictId\", SearchOperationEnum.Equal);\n\n                Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.SubcountyId, \"SubCountyId\", SearchOperationEnum.Equal);\n\n                Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.ParishId, \"ParishId\", SearchOperationEnum.Equal);\n\n                if (inboundDto?.IsActive != null)\n                    Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? \"True\" : \"False\", \"IsActive\", SearchOperationEnum.Equal);\n\n            }\n\n            return filters;\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/ProgramService.cs b/eSusInsurers/Services/Implementations/ProgramService.cs
--- a/eSusInsurers/Services/Implementations/ProgramService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/ProgramService.cs	(date 1732264029123)
@@ -1,9 +1,10 @@
+using System.Linq.Dynamic.Core;
+using System.Linq.Expressions;
 using AutoMapper;
 using AutoMapper.QueryableExtensions;
 using eSusInsurers.Common.Exceptions;
 using eSusInsurers.Constants;
 using eSusInsurers.Infrastructure.Common;
-using eSusInsurers.Infrastructure.Interfaces;
 using eSusInsurers.Models.enums;
 using eSusInsurers.Models.Extensions;
 using eSusInsurers.Models.Helpers;
@@ -11,189 +12,191 @@
 using eSusInsurers.Services.Common;
 using eSusInsurers.Services.Interfaces;
 using Microsoft.EntityFrameworkCore;
-using System.Linq.Dynamic.Core;
-using System.Linq.Expressions;
 
-namespace eSusInsurers.Services.Implementations
-{
-    public class ProgramService(IUnitOfWork unitOfWork,
-                             IMapper mapper) : IProgramsService
-    {
-
-        public async Task<Models.Common.PagedResult<ProgramsModel>> GetPrograms(GetProgramsQuery request, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = ProgarmsFilters(request);
-            Expression<Func<eSusInsurers.Domain.Entities.Program, bool>> predicate = ExpressionBuilder<eSusInsurers.Domain.Entities.Program>.BuildFilterExpression(filters);
-            Dictionary<string, Models.Common.Filter> paginationFilters = FilterHelper.CreatePaginationFilters(request.pagingOptions);
-            (int PageSize, int Page) paginationParams = FilterHelper.GetPaginationParams(paginationFilters);
-            Dictionary<string, Models.Common.Filter> orderByFilters = FilterHelper.CreateOrderByFilters(request.sortingOptions);
-            var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);
+namespace eSusInsurers.Services.Implementations;
+
+public class ProgramService(
+    IUnitOfWork unitOfWork,
+    IMapper mapper) : IProgramsService
+{
+    public async Task<Models.Common.PagedResult<ProgramsModel>> GetPrograms(GetProgramsQuery request,
+        CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = ProgarmsFilters(request);
+        Expression<Func<Domain.Entities.Program, bool>> predicate =
+            ExpressionBuilder<Domain.Entities.Program>.BuildFilterExpression(filters);
+        Dictionary<string, Models.Common.Filter> paginationFilters =
+            FilterHelper.CreatePaginationFilters(request.pagingOptions);
+        var paginationParams = FilterHelper.GetPaginationParams(paginationFilters);
+        Dictionary<string, Models.Common.Filter> orderByFilters =
+            FilterHelper.CreateOrderByFilters(request.sortingOptions);
+        var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);
 
-            var query = unitOfWork.ProgramRepository.GetAll(
-                new string[]
-                   {
-                        "Region", "District", "SubCounty", "Parish"
-                   })
-               .Where(predicate)
-               .OrderByDescending(x => x.Id)
-               .ProjectTo<ProgramsModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.ProgramRepository.GetAll(
+                new[]
+                {
+                    "Region", "District", "SubCounty", "Parish"
+                })
+            .Where(predicate)
+            .OrderByDescending(x => x.Id)
+            .ProjectTo<ProgramsModel>(mapper.ConfigurationProvider);
 
-            if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) && orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == "descending")
-            {
-                orderByParams += ApplicationConstants.descending;
-            }
+        if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) &&
+            orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == "descending")
+            orderByParams += ApplicationConstants.descending;
 
-            if (!string.IsNullOrEmpty(orderByParams))
-            {
-                query = query.OrderBy(orderByParams);
-            }
+        if (!string.IsNullOrEmpty(orderByParams)) query = query.OrderBy(orderByParams);
 
-            var programs = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);
+        var programs = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);
 
-            return new Models.Common.PagedResult<ProgramsModel>
-            {
-                PageSize = paginationParams.PageSize,
-                TotalPages = programs.TotalPages,
-                TotalRecordCount = programs.TotalRecordCount,
-                Records = programs.Records,
-                CurrentPage = paginationParams.Page
-            };
-
-        }
+        return new Models.Common.PagedResult<ProgramsModel>
+        {
+            PageSize = paginationParams.PageSize,
+            TotalPages = programs.TotalPages,
+            TotalRecordCount = programs.TotalRecordCount,
+            Records = programs.Records,
+            CurrentPage = paginationParams.Page
+        };
+    }
 
-        public async Task<bool> AddProgram(ProgramRequest request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
+    public async Task<bool> AddProgram(ProgramRequest request, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
 
-            var program = await unitOfWork.ProgramRepository.GetAll()
-                .Where(x => x.ProgramName == request.ProgramName)
-                .FirstOrDefaultAsync(cancellationToken);
+        var program = await unitOfWork.ProgramRepository.GetAll()
+            .Where(x => x.ProgramName == request.ProgramName)
+            .FirstOrDefaultAsync(cancellationToken);
 
-            if (program != null)
-                throw new Exception($"Program Name ({request.ProgramName}) already exists.");
+        if (program != null)
+            throw new Exception($"Program Name ({request.ProgramName}) already exists.");
 
-            program = mapper.Map<eSusInsurers.Domain.Entities.Program>(request);
+        program = mapper.Map<Domain.Entities.Program>(request);
 
-            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                await unitOfWork.ProgramRepository.AddAsync(program, cancellationToken);
+        try
+        {
+            await unitOfWork.ProgramRepository.AddAsync(program, cancellationToken);
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync(cancellationToken);
-
-            }
-            catch (Exception)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            await transaction.CommitAsync(cancellationToken);
+        }
+        catch (Exception)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
+            throw;
+        }
 
-            return true;
-        }
+        return true;
+    }
 
-        public async Task UpdateProgram(int programId, ProgramRequest request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
+    public async Task UpdateProgram(int programId, ProgramRequest request, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
 
-            ArgumentNullException.ThrowIfNull(programId, nameof(programId));
+        ArgumentNullException.ThrowIfNull(programId, nameof(programId));
 
-            var programs = await unitOfWork.ProgramRepository.GetAll()
-                .Where(x => x.ProgramName == request.ProgramName)
-                .ToListAsync(cancellationToken);
+        var programs = await unitOfWork.ProgramRepository.GetAll()
+            .Where(x => x.ProgramName == request.ProgramName)
+            .ToListAsync(cancellationToken);
 
-            if (programs.Any(x => x.Id != programId))
-                throw new BadRequestException("Program Name and Institution Name already exists.");
+        if (programs.Any(x => x.Id != programId))
+            throw new BadRequestException("Program Name and Institution Name already exists.");
 
-            var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);
+        var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);
 
-            if (program == null)
-                throw new NotFoundException("Program Id doesn't exist.");
+        if (program == null)
+            throw new NotFoundException("Program Id doesn't exist.");
 
-            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                mapper.Map(request, program);
+        try
+        {
+            mapper.Map(request, program);
 
-                await unitOfWork.ProgramRepository.UpdateAsync(program, cancellationToken);
+            await unitOfWork.ProgramRepository.UpdateAsync(program, cancellationToken);
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync(cancellationToken);
-            }
-            catch (Exception)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            await transaction.CommitAsync(cancellationToken);
+        }
+        catch (Exception)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
-        }
+            throw;
+        }
+    }
 
-        public async Task DeleteProgram(int programId, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(programId, nameof(programId));
+    public async Task DeleteProgram(int programId, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(programId, nameof(programId));
 
-            var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);
+        var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);
 
-            if (program == null)
-                throw new NotFoundException($"Program Id: ({programId}) doesn't exist.");
+        if (program == null)
+            throw new NotFoundException($"Program Id: ({programId}) doesn't exist.");
 
-            program.IsActive = false;
+        program.IsActive = false;
 
-            await unitOfWork.SaveChangesAsync(cancellationToken);
-        }
+        await unitOfWork.SaveChangesAsync(cancellationToken);
+    }
 
-        public async Task ActivateProgram(int programId, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(programId, nameof(programId));
+    public async Task ActivateProgram(int programId, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(programId, nameof(programId));
 
-            var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);
+        var program = await unitOfWork.ProgramRepository.GetByIdAsync(programId, cancellationToken: cancellationToken);
 
-            if (program == null)
-                throw new NotFoundException($"Program Id: ({programId}) doesn't exist.");
+        if (program == null)
+            throw new NotFoundException($"Program Id: ({programId}) doesn't exist.");
 
-            program.IsActive = true;
+        program.IsActive = true;
 
-            await unitOfWork.SaveChangesAsync(cancellationToken);
-        }
+        await unitOfWork.SaveChangesAsync(cancellationToken);
+    }
 
 
-        private static Dictionary<string, Models.Common.Filter> ProgarmsFilters(GetProgramsQuery invQuery)
-        {
-            var inboundDto = invQuery.filter;
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    private static Dictionary<string, Models.Common.Filter> ProgarmsFilters(GetProgramsQuery invQuery)
+    {
+        var inboundDto = invQuery.filter;
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            if (inboundDto != null)
-            {
-
-                Filters.AddFilterIfNotEmpty(filters, inboundDto?.ProgramName, "ProgramName", SearchOperationEnum.Contains);
+        if (inboundDto != null)
+        {
+            Filters.AddFilterIfNotEmpty(filters, inboundDto?.ProgramName, "ProgramName", SearchOperationEnum.Contains);
 
-                Filters.AddFilterIfNotEmpty(filters, inboundDto?.RegionName, "Region.RegionName", SearchOperationEnum.Contains);
+            Filters.AddFilterIfNotEmpty(filters, inboundDto?.RegionName, "Region.RegionName",
+                SearchOperationEnum.Contains);
 
-                Filters.AddFilterIfNotEmpty(filters, inboundDto?.DistrictName, "District.DistrictName", SearchOperationEnum.Contains);
+            Filters.AddFilterIfNotEmpty(filters, inboundDto?.DistrictName, "District.DistrictName",
+                SearchOperationEnum.Contains);
 
-                Filters.AddFilterIfNotEmpty(filters, inboundDto?.SubCountyName, "SubCounty.SubCountyName", SearchOperationEnum.Contains);
+            Filters.AddFilterIfNotEmpty(filters, inboundDto?.SubCountyName, "SubCounty.SubCountyName",
+                SearchOperationEnum.Contains);
 
-                Filters.AddFilterIfNotEmpty(filters, inboundDto?.ParishName, "Parish.ParishName", SearchOperationEnum.Contains);
+            Filters.AddFilterIfNotEmpty(filters, inboundDto?.ParishName, "Parish.ParishName",
+                SearchOperationEnum.Contains);
 
-                Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.RegionId, "RegionId", SearchOperationEnum.Equal);
+            Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.RegionId, "RegionId",
+                SearchOperationEnum.Equal);
 
-                Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.DistrictId, "DistrictId", SearchOperationEnum.Equal);
+            Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.DistrictId, "DistrictId",
+                SearchOperationEnum.Equal);
 
-                Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.SubcountyId, "SubCountyId", SearchOperationEnum.Equal);
+            Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.SubcountyId, "SubCountyId",
+                SearchOperationEnum.Equal);
 
-                Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.ParishId, "ParishId", SearchOperationEnum.Equal);
+            Filters.AddFilterIfValueGreaterThanZero(filters, inboundDto?.ParishId, "ParishId",
+                SearchOperationEnum.Equal);
 
-                if (inboundDto?.IsActive != null)
-                    Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? "True" : "False", "IsActive", SearchOperationEnum.Equal);
-
-            }
+            if (inboundDto?.IsActive != null)
+                Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? "True" : "False", "IsActive",
+                    SearchOperationEnum.Equal);
+        }
 
-            return filters;
-        }
+        return filters;
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Options/ConfigureSwaggerOptions.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using Asp.Versioning.ApiExplorer;\nusing Microsoft.Extensions.Options;\nusing Microsoft.OpenApi.Models;\nusing Swashbuckle.AspNetCore.Filters;\nusing Swashbuckle.AspNetCore.SwaggerGen;\n\nnamespace eSusInsurers.Options\n{\n#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member\n    public class ConfigureSwaggerOptions : IConfigureNamedOptions<SwaggerGenOptions>\n#pragma warning restore CS1591 // Missing XML comment for publicly visible type or member\n    {\n        private readonly IApiVersionDescriptionProvider _provider;\n\n#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member\n        public ConfigureSwaggerOptions(IApiVersionDescriptionProvider provider)\n#pragma warning restore CS1591 // Missing XML comment for publicly visible type or member\n        {\n            _provider = provider;\n        }\n\n#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member\n        public void Configure(string? name, SwaggerGenOptions options)\n#pragma warning restore CS1591 // Missing XML comment for publicly visible type or member\n        {\n            Configure(options);\n        }\n\n#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member\n        public void Configure(SwaggerGenOptions options)\n#pragma warning restore CS1591 // Missing XML comment for publicly visible type or member\n        {\n            // add swagger document for every API version discovered\n            foreach (var description in _provider.ApiVersionDescriptions)\n            {\n                if (!options.SwaggerGeneratorOptions.SwaggerDocs.ContainsKey(description.GroupName))\n                {\n                    options.SwaggerDoc(\n                        description.GroupName,\n                        CreateVersionInfo(description));\n                }\n            }\n\n            // Set the comments path for the Swagger JSON and UI.\n            var xmlFile = $\"eSusInsurers.xml\";\n            var xmlPath = Path.Combine(AppContext.BaseDirectory, xmlFile);\n            options.IncludeXmlComments(xmlPath, true);\n\n            // add example filters\n            options.ExampleFilters();\n        }\n\n        private OpenApiInfo CreateVersionInfo(ApiVersionDescription description)\n        {\n            var info = new OpenApiInfo()\n            {\n                Title = \"eSusInsurer API\",\n                Version = description.ApiVersion.ToString(),\n                Description = \"A RESTful API for managing eSus Insurance Module.\"\n            };\n\n            if (description.IsDeprecated)\n            {\n                info.Description += \" This API version has been deprecated.\";\n            }\n\n            return info;\n        }\n\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Options/ConfigureSwaggerOptions.cs b/eSusInsurers/Options/ConfigureSwaggerOptions.cs
--- a/eSusInsurers/Options/ConfigureSwaggerOptions.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Options/ConfigureSwaggerOptions.cs	(date 1732264029211)
@@ -4,68 +4,58 @@
 using Swashbuckle.AspNetCore.Filters;
 using Swashbuckle.AspNetCore.SwaggerGen;
 
-namespace eSusInsurers.Options
-{
+namespace eSusInsurers.Options;
 #pragma warning disable CS1591 // Missing XML comment for publicly visible type or member
-    public class ConfigureSwaggerOptions : IConfigureNamedOptions<SwaggerGenOptions>
+public class ConfigureSwaggerOptions : IConfigureNamedOptions<SwaggerGenOptions>
 #pragma warning restore CS1591 // Missing XML comment for publicly visible type or member
-    {
-        private readonly IApiVersionDescriptionProvider _provider;
+{
+    private readonly IApiVersionDescriptionProvider _provider;
 
 #pragma warning disable CS1591 // Missing XML comment for publicly visible type or member
-        public ConfigureSwaggerOptions(IApiVersionDescriptionProvider provider)
+    public ConfigureSwaggerOptions(IApiVersionDescriptionProvider provider)
 #pragma warning restore CS1591 // Missing XML comment for publicly visible type or member
-        {
-            _provider = provider;
-        }
+    {
+        _provider = provider;
+    }
 
 #pragma warning disable CS1591 // Missing XML comment for publicly visible type or member
-        public void Configure(string? name, SwaggerGenOptions options)
+    public void Configure(string? name, SwaggerGenOptions options)
 #pragma warning restore CS1591 // Missing XML comment for publicly visible type or member
-        {
-            Configure(options);
-        }
+    {
+        Configure(options);
+    }
 
 #pragma warning disable CS1591 // Missing XML comment for publicly visible type or member
-        public void Configure(SwaggerGenOptions options)
+    public void Configure(SwaggerGenOptions options)
 #pragma warning restore CS1591 // Missing XML comment for publicly visible type or member
-        {
-            // add swagger document for every API version discovered
-            foreach (var description in _provider.ApiVersionDescriptions)
-            {
-                if (!options.SwaggerGeneratorOptions.SwaggerDocs.ContainsKey(description.GroupName))
-                {
-                    options.SwaggerDoc(
-                        description.GroupName,
-                        CreateVersionInfo(description));
-                }
-            }
+    {
+        // add swagger document for every API version discovered
+        foreach (var description in _provider.ApiVersionDescriptions)
+            if (!options.SwaggerGeneratorOptions.SwaggerDocs.ContainsKey(description.GroupName))
+                options.SwaggerDoc(
+                    description.GroupName,
+                    CreateVersionInfo(description));
 
-            // Set the comments path for the Swagger JSON and UI.
-            var xmlFile = $"eSusInsurers.xml";
-            var xmlPath = Path.Combine(AppContext.BaseDirectory, xmlFile);
-            options.IncludeXmlComments(xmlPath, true);
+        // Set the comments path for the Swagger JSON and UI.
+        var xmlFile = "eSusInsurers.xml";
+        var xmlPath = Path.Combine(AppContext.BaseDirectory, xmlFile);
+        options.IncludeXmlComments(xmlPath, true);
 
-            // add example filters
-            options.ExampleFilters();
-        }
+        // add example filters
+        options.ExampleFilters();
+    }
 
-        private OpenApiInfo CreateVersionInfo(ApiVersionDescription description)
-        {
-            var info = new OpenApiInfo()
-            {
-                Title = "eSusInsurer API",
-                Version = description.ApiVersion.ToString(),
-                Description = "A RESTful API for managing eSus Insurance Module."
-            };
+    private OpenApiInfo CreateVersionInfo(ApiVersionDescription description)
+    {
+        var info = new OpenApiInfo
+        {
+            Title = "eSusInsurer API",
+            Version = description.ApiVersion.ToString(),
+            Description = "A RESTful API for managing eSus Insurance Module."
+        };
 
-            if (description.IsDeprecated)
-            {
-                info.Description += " This API version has been deprecated.";
-            }
+        if (description.IsDeprecated) info.Description += " This API version has been deprecated.";
 
-            return info;
-        }
-
+        return info;
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/RolesService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing AutoMapper.QueryableExtensions;\nusing eSusInsurers.Common.Exceptions;\nusing eSusInsurers.Constants;\nusing eSusInsurers.Domain.Entities;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Models.enums;\nusing eSusInsurers.Models.Extensions;\nusing eSusInsurers.Models.Helpers;\nusing eSusInsurers.Models.Roles.GetRoles;\nusing eSusInsurers.Models.Roles.RoleDetails;\nusing eSusInsurers.Services.Common;\nusing eSusInsurers.Services.Interfaces;\nusing Microsoft.EntityFrameworkCore.Storage;\nusing Newtonsoft.Json;\nusing System.Linq.Dynamic.Core;\nusing System.Linq.Expressions;\nusing WMS.Models.Roles;\nusing WMS.Models.Roles.UpdateRole;\n\nnamespace eSusInsurers.Services.Implementations\n{\n    public class RolesService(IUnitOfWork unitOfWork, IMapper mapper) : IRolesService\n    {\n        public async Task<List<ApplicationMenuItems>?> GetApplicationMenuItems(CancellationToken cancellationToken)\n        {\n            var result = await unitOfWork.ApplicationMenuRepository.GetApplicationMenuList(cancellationToken);\n\n            if (result == null)\n                throw new ApplicationException();\n\n            return JsonConvert.DeserializeObject<List<ApplicationMenuItems>>(result);\n        }\n\n        public async Task<Models.Common.PagedResult<RoleModel>> GetRoles(GetRolesQuery request, CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = RolesFilters(request);\n            Expression<Func<Role, bool>> predicate = ExpressionBuilder<Role>.BuildFilterExpression(filters);\n            Dictionary<string, Models.Common.Filter> paginationFilters = FilterHelper.CreatePaginationFilters(request.pagingOptions);\n            (int PageSize, int Page) paginationParams = FilterHelper.GetPaginationParams(paginationFilters);\n            Dictionary<string, Models.Common.Filter> orderByFilters = FilterHelper.CreateOrderByFilters(request.sortingOptions);\n            var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);\n\n            var query = unitOfWork.RoleRepository.GetAll(\n                   new string[]\n                   {\n                       \"ReportingTo\"\n                   })\n               .Where(predicate)\n               .ProjectTo<RoleModel>(mapper.ConfigurationProvider);\n\n            if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) && orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == \"descending\")\n            {\n                orderByParams += ApplicationConstants.descending;\n            }\n\n            if (!string.IsNullOrEmpty(orderByParams))\n            {\n                query = query.OrderBy(orderByParams);\n            }\n\n            var roles = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);\n\n            return new Models.Common.PagedResult<RoleModel>\n            {\n                PageSize = paginationParams.PageSize,\n                TotalPages = roles.TotalPages,\n                TotalRecordCount = roles.TotalRecordCount,\n                Records = roles.Records,\n                CurrentPage = paginationParams.Page\n            };\n\n        }\n\n        public async Task<long> AddRole(RoleRequest request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n\n            var role = await unitOfWork.RoleRepository.GetByRoleNameAsync(request.RoleName, cancellationToken);\n\n            if (role != null)\n                throw new BadRequestException($\"Role name: ({request.RoleName}) already exists.\");\n\n            role = mapper.Map<Role>(request);\n\n            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                var entity = await unitOfWork.RoleRepository.AddAsync(role, cancellationToken);\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await AddMenuRolesPrivilege(entity.Id, transaction, cancellationToken);\n\n                await transaction.CommitAsync(cancellationToken);\n\n                return entity.Id;\n            }\n            catch (Exception ex)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n        }\n\n        public async Task UpdateRole(int roleId, UpdateRoleRequestModel request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n\n            ArgumentNullException.ThrowIfNull(roleId, nameof(roleId));\n\n            var role = await unitOfWork.RoleRepository.GetByIdAsync(roleId, null, false, cancellationToken);\n\n            if (role == null)\n                throw new NotFoundException(\"Role Id doesn't exist.\");\n\n            var roleName = await unitOfWork.RoleRepository.GetByRoleNameAsync(roleId, request.RoleName, cancellationToken);\n\n            if (roleName != null)\n                throw new BadRequestException($\"Role name: ({request.RoleName}) already exists.\");\n\n            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);\n\n            try\n            {\n                mapper.Map(request, role);\n\n                await unitOfWork.RoleRepository.UpdateAsync(role, cancellationToken);\n\n                await UpdateMenuRolesPrivilege(roleId, request.MenuRolesPrivileges, transaction, cancellationToken);\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n\n                await transaction.CommitAsync(cancellationToken);\n            }\n            catch (Exception ex)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n                throw;\n            }\n        }\n\n        public async Task<RoleDetails?> RoleDetails(int roleId, CancellationToken cancellationToken)\n        {\n            var result = await unitOfWork.RoleRepository.GetRoleDetails(roleId, cancellationToken);\n\n            if (result == null)\n                throw new ApplicationException();\n\n            var roleDetails = mapper.Map<RoleDetails>(result);\n\n            roleDetails.MenuRolePrivileges = JsonConvert.DeserializeObject<List<MenuRolePrivileges>>(result.ApplicationMenuRolePrivileges);\n\n            return roleDetails;\n        }\n\n        private async Task AddMenuRolesPrivilege(int roleId, IDbContextTransaction transaction, CancellationToken cancellationToken)\n        {\n            try\n            {\n                var result = await unitOfWork.ApplicationMenuRepository.GetApplicationMenuList(cancellationToken);\n\n                if (result == null)\n                    throw new ApplicationException();\n\n                var query = JsonConvert.DeserializeObject<List<ApplicationMenuItems>>(result);\n\n                foreach (var menuItem in query)\n                {\n                    var menuRolesPrivileges = mapper.Map<MenuRolesPrivilege>(menuItem);\n                    menuRolesPrivileges.RoleId = roleId;\n\n                    await unitOfWork.MenuRolesPrivilegeRepository.AddAsync(menuRolesPrivileges, cancellationToken);\n\n                    if (menuItem.ApplicationChildMenuItems.Count > 0)\n                    {\n                        foreach (var childMenuItem in menuItem.ApplicationChildMenuItems)\n                        {\n                            var childMenuRolesPrivileges = mapper.Map<MenuRolesPrivilege>(childMenuItem);\n                            childMenuRolesPrivileges.ApplicationMenuId = menuItem.ApplicationMenuId;\n                            childMenuRolesPrivileges.RoleId = roleId;\n\n                            await unitOfWork.MenuRolesPrivilegeRepository.AddAsync(childMenuRolesPrivileges, cancellationToken);\n\n                            if (childMenuItem.ApplicationFunctionalities.Count > 0)\n                            {\n                                foreach (var applicationFunctionality in childMenuItem.ApplicationFunctionalities)\n                                {\n                                    var menuRolesFunctionality = mapper.Map<MenuRolesFunctionality>(applicationFunctionality);\n                                    menuRolesFunctionality.RoleId = roleId;\n\n                                    await unitOfWork.MenuRolesFunctionalityRepository.AddAsync(menuRolesFunctionality, cancellationToken);\n\n                                    foreach (var approvalProcesses in applicationFunctionality.FunctionalityApprovalProcessList)\n                                    {\n                                        var functionalityApprovalProcesses = mapper.Map<Domain.Entities.MenuRoleFunctionalityApprovalProcess>(approvalProcesses);\n                                        functionalityApprovalProcesses.RoleId = roleId;\n\n                                        await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.AddAsync(functionalityApprovalProcesses, cancellationToken);\n                                    }\n                                }\n                            }\n                        }\n                    }\n\n                    if (menuItem.ApplicationFunctionalities.Count > 0)\n                    {\n                        foreach (var applicationFunctionality in menuItem.ApplicationFunctionalities)\n                        {\n                            var menuRolesFunctionality = mapper.Map<MenuRolesFunctionality>(applicationFunctionality);\n                            menuRolesFunctionality.RoleId = roleId;\n\n                            await unitOfWork.MenuRolesFunctionalityRepository.AddAsync(menuRolesFunctionality, cancellationToken);\n\n                            foreach (var approvalProcesses in applicationFunctionality.FunctionalityApprovalProcessList)\n                            {\n                                var functionalityApprovalProcesses = mapper.Map<Domain.Entities.MenuRoleFunctionalityApprovalProcess>(approvalProcesses);\n                                functionalityApprovalProcesses.RoleId = roleId;\n\n                                await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.AddAsync(functionalityApprovalProcesses, cancellationToken);\n                            }\n                        }\n                    }\n                }\n\n                await unitOfWork.SaveChangesAsync(cancellationToken);\n            }\n            catch (Exception)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n        }\n\n        private async Task UpdateMenuRolesPrivilege(int roleId, List<MenuRolesPrivilegeItems> menuRolesPrivilegeItems, IDbContextTransaction transaction, CancellationToken cancellationToken)\n        {\n            try\n            {\n                var menuRolesPrivileges = await unitOfWork.MenuRolesPrivilegeRepository.GetAllRoleIdAsync(roleId, cancellationToken);\n\n                var menuRolesFunctionalities = await unitOfWork.MenuRolesFunctionalityRepository.GetAllRoleIdAsync(roleId, cancellationToken);\n\n                var menuRoleFunctionalityApprovals = await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.GetAllByRoleIdAsync(roleId, cancellationToken);\n\n                foreach (var menuItem in menuRolesPrivilegeItems)\n                {\n                    var menuRolesPrivilege = menuRolesPrivileges.Where(x => x.Id == menuItem.MenuRolesPrivilegeId).FirstOrDefault();\n                    if (menuRolesPrivilege != null)\n                    {\n                        mapper.Map(menuItem, menuRolesPrivilege);\n                        menuRolesPrivilege.RoleId = roleId;\n\n                        await unitOfWork.MenuRolesPrivilegeRepository.UpdateAsync(menuRolesPrivilege, cancellationToken);\n                    }\n\n                    if (menuItem.MenuRolesFunctionalities.Count > 0)\n                    {\n                        foreach (var menuFunctionality in menuItem.MenuRolesFunctionalities)\n                        {\n                            var functionality = menuRolesFunctionalities.Where(x => x.Id == menuFunctionality.MenuRolesFunctionalityId).FirstOrDefault();\n                            if (functionality != null)\n                            {\n                                mapper.Map(menuFunctionality, functionality);\n                                functionality.RoleId = roleId;\n\n                                await unitOfWork.MenuRolesFunctionalityRepository.UpdateAsync(functionality, cancellationToken);\n                            }\n\n                            if (menuFunctionality.MenuRoleFunctionalityApprovalProcess.Count > 0)\n                            {\n                                foreach (var applicationFunctionality in menuFunctionality.MenuRoleFunctionalityApprovalProcess)\n                                {\n                                    var menuRoleFunctionality = menuRoleFunctionalityApprovals.Where(x => x.Id == applicationFunctionality.MenuRoleFunctionalityApprovalProcessId).FirstOrDefault();\n                                    if (menuRoleFunctionality != null)\n                                    {\n                                        mapper.Map(applicationFunctionality, menuRoleFunctionality);\n                                        menuRoleFunctionality.RoleId = roleId;\n\n                                        await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.UpdateAsync(menuRoleFunctionality, cancellationToken);\n                                    }\n                                }\n                            }\n                        }\n                    }\n                }\n            }\n            catch (Exception)\n            {\n                await transaction.RollbackAsync(cancellationToken);\n\n                throw;\n            }\n        }\n\n        #region Private Methods\n        private static Dictionary<string, Models.Common.Filter> RolesFilters(GetRolesQuery invQuery)\n        {\n            var inboundDto = invQuery.filter;\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            if (inboundDto != null)\n            {\n                if (inboundDto?.IsActive != null)\n                    Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? \"True\" : \"False\", \"IsActive\", SearchOperationEnum.Equal);\n\n            }\n\n            return filters;\n        }\n        #endregion\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/RolesService.cs b/eSusInsurers/Services/Implementations/RolesService.cs
--- a/eSusInsurers/Services/Implementations/RolesService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/RolesService.cs	(date 1732264029129)
@@ -1,3 +1,5 @@
+using System.Linq.Dynamic.Core;
+using System.Linq.Expressions;
 using AutoMapper;
 using AutoMapper.QueryableExtensions;
 using eSusInsurers.Common.Exceptions;
@@ -13,303 +15,312 @@
 using eSusInsurers.Services.Interfaces;
 using Microsoft.EntityFrameworkCore.Storage;
 using Newtonsoft.Json;
-using System.Linq.Dynamic.Core;
-using System.Linq.Expressions;
 using WMS.Models.Roles;
 using WMS.Models.Roles.UpdateRole;
+using MenuRoleFunctionalityApprovalProcess = eSusInsurers.Domain.Entities.MenuRoleFunctionalityApprovalProcess;
 
-namespace eSusInsurers.Services.Implementations
-{
-    public class RolesService(IUnitOfWork unitOfWork, IMapper mapper) : IRolesService
-    {
-        public async Task<List<ApplicationMenuItems>?> GetApplicationMenuItems(CancellationToken cancellationToken)
-        {
-            var result = await unitOfWork.ApplicationMenuRepository.GetApplicationMenuList(cancellationToken);
+namespace eSusInsurers.Services.Implementations;
+
+public class RolesService(IUnitOfWork unitOfWork, IMapper mapper) : IRolesService
+{
+    public async Task<List<ApplicationMenuItems>?> GetApplicationMenuItems(CancellationToken cancellationToken)
+    {
+        var result = await unitOfWork.ApplicationMenuRepository.GetApplicationMenuList(cancellationToken);
 
-            if (result == null)
-                throw new ApplicationException();
+        if (result == null)
+            throw new ApplicationException();
 
-            return JsonConvert.DeserializeObject<List<ApplicationMenuItems>>(result);
-        }
+        return JsonConvert.DeserializeObject<List<ApplicationMenuItems>>(result);
+    }
 
-        public async Task<Models.Common.PagedResult<RoleModel>> GetRoles(GetRolesQuery request, CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = RolesFilters(request);
-            Expression<Func<Role, bool>> predicate = ExpressionBuilder<Role>.BuildFilterExpression(filters);
-            Dictionary<string, Models.Common.Filter> paginationFilters = FilterHelper.CreatePaginationFilters(request.pagingOptions);
-            (int PageSize, int Page) paginationParams = FilterHelper.GetPaginationParams(paginationFilters);
-            Dictionary<string, Models.Common.Filter> orderByFilters = FilterHelper.CreateOrderByFilters(request.sortingOptions);
-            var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);
+    public async Task<Models.Common.PagedResult<RoleModel>> GetRoles(GetRolesQuery request,
+        CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = RolesFilters(request);
+        Expression<Func<Role, bool>> predicate = ExpressionBuilder<Role>.BuildFilterExpression(filters);
+        Dictionary<string, Models.Common.Filter> paginationFilters =
+            FilterHelper.CreatePaginationFilters(request.pagingOptions);
+        var paginationParams = FilterHelper.GetPaginationParams(paginationFilters);
+        Dictionary<string, Models.Common.Filter> orderByFilters =
+            FilterHelper.CreateOrderByFilters(request.sortingOptions);
+        var orderByParams = OrderByHelper.GetOrderByParams(orderByFilters);
 
-            var query = unitOfWork.RoleRepository.GetAll(
-                   new string[]
-                   {
-                       "ReportingTo"
-                   })
-               .Where(predicate)
-               .ProjectTo<RoleModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.RoleRepository.GetAll(
+                new[]
+                {
+                    "ReportingTo"
+                })
+            .Where(predicate)
+            .ProjectTo<RoleModel>(mapper.ConfigurationProvider);
 
-            if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) && orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == "descending")
-            {
-                orderByParams += ApplicationConstants.descending;
-            }
+        if (!string.IsNullOrEmpty(orderByParams) && orderByFilters.ContainsKey(ApplicationConstants.sortBy) &&
+            orderByFilters[ApplicationConstants.sortBy].Value.ToLower() == "descending")
+            orderByParams += ApplicationConstants.descending;
 
-            if (!string.IsNullOrEmpty(orderByParams))
-            {
-                query = query.OrderBy(orderByParams);
-            }
+        if (!string.IsNullOrEmpty(orderByParams)) query = query.OrderBy(orderByParams);
 
-            var roles = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);
+        var roles = query.ToPagedResult(paginationParams.Page, paginationParams.PageSize);
 
-            return new Models.Common.PagedResult<RoleModel>
-            {
-                PageSize = paginationParams.PageSize,
-                TotalPages = roles.TotalPages,
-                TotalRecordCount = roles.TotalRecordCount,
-                Records = roles.Records,
-                CurrentPage = paginationParams.Page
-            };
-
-        }
+        return new Models.Common.PagedResult<RoleModel>
+        {
+            PageSize = paginationParams.PageSize,
+            TotalPages = roles.TotalPages,
+            TotalRecordCount = roles.TotalRecordCount,
+            Records = roles.Records,
+            CurrentPage = paginationParams.Page
+        };
+    }
 
-        public async Task<long> AddRole(RoleRequest request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
+    public async Task<long> AddRole(RoleRequest request, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
 
-            var role = await unitOfWork.RoleRepository.GetByRoleNameAsync(request.RoleName, cancellationToken);
+        var role = await unitOfWork.RoleRepository.GetByRoleNameAsync(request.RoleName, cancellationToken);
 
-            if (role != null)
-                throw new BadRequestException($"Role name: ({request.RoleName}) already exists.");
+        if (role != null)
+            throw new BadRequestException($"Role name: ({request.RoleName}) already exists.");
 
-            role = mapper.Map<Role>(request);
+        role = mapper.Map<Role>(request);
 
-            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                var entity = await unitOfWork.RoleRepository.AddAsync(role, cancellationToken);
+        try
+        {
+            var entity = await unitOfWork.RoleRepository.AddAsync(role, cancellationToken);
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await AddMenuRolesPrivilege(entity.Id, transaction, cancellationToken);
+            await AddMenuRolesPrivilege(entity.Id, transaction, cancellationToken);
 
-                await transaction.CommitAsync(cancellationToken);
+            await transaction.CommitAsync(cancellationToken);
 
-                return entity.Id;
-            }
-            catch (Exception ex)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            return entity.Id;
+        }
+        catch (Exception ex)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
-        }
+            throw;
+        }
+    }
 
-        public async Task UpdateRole(int roleId, UpdateRoleRequestModel request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
+    public async Task UpdateRole(int roleId, UpdateRoleRequestModel request, CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
 
-            ArgumentNullException.ThrowIfNull(roleId, nameof(roleId));
+        ArgumentNullException.ThrowIfNull(roleId, nameof(roleId));
 
-            var role = await unitOfWork.RoleRepository.GetByIdAsync(roleId, null, false, cancellationToken);
+        var role = await unitOfWork.RoleRepository.GetByIdAsync(roleId, null, false, cancellationToken);
 
-            if (role == null)
-                throw new NotFoundException("Role Id doesn't exist.");
+        if (role == null)
+            throw new NotFoundException("Role Id doesn't exist.");
 
-            var roleName = await unitOfWork.RoleRepository.GetByRoleNameAsync(roleId, request.RoleName, cancellationToken);
+        var roleName = await unitOfWork.RoleRepository.GetByRoleNameAsync(roleId, request.RoleName, cancellationToken);
 
-            if (roleName != null)
-                throw new BadRequestException($"Role name: ({request.RoleName}) already exists.");
+        if (roleName != null)
+            throw new BadRequestException($"Role name: ({request.RoleName}) already exists.");
 
-            var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
+        var transaction = await unitOfWork.BeginTransactionAsync(cancellationToken);
 
-            try
-            {
-                mapper.Map(request, role);
+        try
+        {
+            mapper.Map(request, role);
 
-                await unitOfWork.RoleRepository.UpdateAsync(role, cancellationToken);
+            await unitOfWork.RoleRepository.UpdateAsync(role, cancellationToken);
 
-                await UpdateMenuRolesPrivilege(roleId, request.MenuRolesPrivileges, transaction, cancellationToken);
+            await UpdateMenuRolesPrivilege(roleId, request.MenuRolesPrivileges, transaction, cancellationToken);
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
 
-                await transaction.CommitAsync(cancellationToken);
-            }
-            catch (Exception ex)
-            {
-                await transaction.RollbackAsync(cancellationToken);
-                throw;
-            }
-        }
+            await transaction.CommitAsync(cancellationToken);
+        }
+        catch (Exception ex)
+        {
+            await transaction.RollbackAsync(cancellationToken);
+            throw;
+        }
+    }
 
-        public async Task<RoleDetails?> RoleDetails(int roleId, CancellationToken cancellationToken)
-        {
-            var result = await unitOfWork.RoleRepository.GetRoleDetails(roleId, cancellationToken);
+    public async Task<RoleDetails?> RoleDetails(int roleId, CancellationToken cancellationToken)
+    {
+        var result = await unitOfWork.RoleRepository.GetRoleDetails(roleId, cancellationToken);
 
-            if (result == null)
-                throw new ApplicationException();
+        if (result == null)
+            throw new ApplicationException();
 
-            var roleDetails = mapper.Map<RoleDetails>(result);
+        var roleDetails = mapper.Map<RoleDetails>(result);
 
-            roleDetails.MenuRolePrivileges = JsonConvert.DeserializeObject<List<MenuRolePrivileges>>(result.ApplicationMenuRolePrivileges);
+        roleDetails.MenuRolePrivileges =
+            JsonConvert.DeserializeObject<List<MenuRolePrivileges>>(result.ApplicationMenuRolePrivileges);
 
-            return roleDetails;
-        }
+        return roleDetails;
+    }
 
-        private async Task AddMenuRolesPrivilege(int roleId, IDbContextTransaction transaction, CancellationToken cancellationToken)
-        {
-            try
-            {
-                var result = await unitOfWork.ApplicationMenuRepository.GetApplicationMenuList(cancellationToken);
+    private async Task AddMenuRolesPrivilege(int roleId, IDbContextTransaction transaction,
+        CancellationToken cancellationToken)
+    {
+        try
+        {
+            var result = await unitOfWork.ApplicationMenuRepository.GetApplicationMenuList(cancellationToken);
 
-                if (result == null)
-                    throw new ApplicationException();
+            if (result == null)
+                throw new ApplicationException();
 
-                var query = JsonConvert.DeserializeObject<List<ApplicationMenuItems>>(result);
+            var query = JsonConvert.DeserializeObject<List<ApplicationMenuItems>>(result);
 
-                foreach (var menuItem in query)
-                {
-                    var menuRolesPrivileges = mapper.Map<MenuRolesPrivilege>(menuItem);
-                    menuRolesPrivileges.RoleId = roleId;
+            foreach (var menuItem in query)
+            {
+                var menuRolesPrivileges = mapper.Map<MenuRolesPrivilege>(menuItem);
+                menuRolesPrivileges.RoleId = roleId;
 
-                    await unitOfWork.MenuRolesPrivilegeRepository.AddAsync(menuRolesPrivileges, cancellationToken);
+                await unitOfWork.MenuRolesPrivilegeRepository.AddAsync(menuRolesPrivileges, cancellationToken);
 
-                    if (menuItem.ApplicationChildMenuItems.Count > 0)
-                    {
-                        foreach (var childMenuItem in menuItem.ApplicationChildMenuItems)
-                        {
-                            var childMenuRolesPrivileges = mapper.Map<MenuRolesPrivilege>(childMenuItem);
-                            childMenuRolesPrivileges.ApplicationMenuId = menuItem.ApplicationMenuId;
-                            childMenuRolesPrivileges.RoleId = roleId;
+                if (menuItem.ApplicationChildMenuItems.Count > 0)
+                    foreach (var childMenuItem in menuItem.ApplicationChildMenuItems)
+                    {
+                        var childMenuRolesPrivileges = mapper.Map<MenuRolesPrivilege>(childMenuItem);
+                        childMenuRolesPrivileges.ApplicationMenuId = menuItem.ApplicationMenuId;
+                        childMenuRolesPrivileges.RoleId = roleId;
 
-                            await unitOfWork.MenuRolesPrivilegeRepository.AddAsync(childMenuRolesPrivileges, cancellationToken);
+                        await unitOfWork.MenuRolesPrivilegeRepository.AddAsync(childMenuRolesPrivileges,
+                            cancellationToken);
 
-                            if (childMenuItem.ApplicationFunctionalities.Count > 0)
-                            {
-                                foreach (var applicationFunctionality in childMenuItem.ApplicationFunctionalities)
-                                {
-                                    var menuRolesFunctionality = mapper.Map<MenuRolesFunctionality>(applicationFunctionality);
-                                    menuRolesFunctionality.RoleId = roleId;
+                        if (childMenuItem.ApplicationFunctionalities.Count > 0)
+                            foreach (var applicationFunctionality in childMenuItem.ApplicationFunctionalities)
+                            {
+                                var menuRolesFunctionality =
+                                    mapper.Map<MenuRolesFunctionality>(applicationFunctionality);
+                                menuRolesFunctionality.RoleId = roleId;
 
-                                    await unitOfWork.MenuRolesFunctionalityRepository.AddAsync(menuRolesFunctionality, cancellationToken);
+                                await unitOfWork.MenuRolesFunctionalityRepository.AddAsync(menuRolesFunctionality,
+                                    cancellationToken);
 
-                                    foreach (var approvalProcesses in applicationFunctionality.FunctionalityApprovalProcessList)
-                                    {
-                                        var functionalityApprovalProcesses = mapper.Map<Domain.Entities.MenuRoleFunctionalityApprovalProcess>(approvalProcesses);
-                                        functionalityApprovalProcesses.RoleId = roleId;
+                                foreach (var approvalProcesses in applicationFunctionality
+                                             .FunctionalityApprovalProcessList)
+                                {
+                                    var functionalityApprovalProcesses =
+                                        mapper.Map<MenuRoleFunctionalityApprovalProcess>(approvalProcesses);
+                                    functionalityApprovalProcesses.RoleId = roleId;
 
-                                        await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.AddAsync(functionalityApprovalProcesses, cancellationToken);
-                                    }
+                                    await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.AddAsync(
+                                        functionalityApprovalProcesses, cancellationToken);
                                 }
                             }
-                        }
                     }
 
-                    if (menuItem.ApplicationFunctionalities.Count > 0)
-                    {
-                        foreach (var applicationFunctionality in menuItem.ApplicationFunctionalities)
-                        {
-                            var menuRolesFunctionality = mapper.Map<MenuRolesFunctionality>(applicationFunctionality);
-                            menuRolesFunctionality.RoleId = roleId;
+                if (menuItem.ApplicationFunctionalities.Count > 0)
+                    foreach (var applicationFunctionality in menuItem.ApplicationFunctionalities)
+                    {
+                        var menuRolesFunctionality = mapper.Map<MenuRolesFunctionality>(applicationFunctionality);
+                        menuRolesFunctionality.RoleId = roleId;
 
-                            await unitOfWork.MenuRolesFunctionalityRepository.AddAsync(menuRolesFunctionality, cancellationToken);
+                        await unitOfWork.MenuRolesFunctionalityRepository.AddAsync(menuRolesFunctionality,
+                            cancellationToken);
 
-                            foreach (var approvalProcesses in applicationFunctionality.FunctionalityApprovalProcessList)
-                            {
-                                var functionalityApprovalProcesses = mapper.Map<Domain.Entities.MenuRoleFunctionalityApprovalProcess>(approvalProcesses);
-                                functionalityApprovalProcesses.RoleId = roleId;
+                        foreach (var approvalProcesses in applicationFunctionality.FunctionalityApprovalProcessList)
+                        {
+                            var functionalityApprovalProcesses =
+                                mapper.Map<MenuRoleFunctionalityApprovalProcess>(approvalProcesses);
+                            functionalityApprovalProcesses.RoleId = roleId;
 
-                                await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.AddAsync(functionalityApprovalProcesses, cancellationToken);
-                            }
+                            await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.AddAsync(
+                                functionalityApprovalProcesses, cancellationToken);
                         }
                     }
-                }
+            }
 
-                await unitOfWork.SaveChangesAsync(cancellationToken);
-            }
-            catch (Exception)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            await unitOfWork.SaveChangesAsync(cancellationToken);
+        }
+        catch (Exception)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
-        }
+            throw;
+        }
+    }
 
-        private async Task UpdateMenuRolesPrivilege(int roleId, List<MenuRolesPrivilegeItems> menuRolesPrivilegeItems, IDbContextTransaction transaction, CancellationToken cancellationToken)
-        {
-            try
-            {
-                var menuRolesPrivileges = await unitOfWork.MenuRolesPrivilegeRepository.GetAllRoleIdAsync(roleId, cancellationToken);
+    private async Task UpdateMenuRolesPrivilege(int roleId, List<MenuRolesPrivilegeItems> menuRolesPrivilegeItems,
+        IDbContextTransaction transaction, CancellationToken cancellationToken)
+    {
+        try
+        {
+            var menuRolesPrivileges =
+                await unitOfWork.MenuRolesPrivilegeRepository.GetAllRoleIdAsync(roleId, cancellationToken);
 
-                var menuRolesFunctionalities = await unitOfWork.MenuRolesFunctionalityRepository.GetAllRoleIdAsync(roleId, cancellationToken);
+            var menuRolesFunctionalities =
+                await unitOfWork.MenuRolesFunctionalityRepository.GetAllRoleIdAsync(roleId, cancellationToken);
 
-                var menuRoleFunctionalityApprovals = await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.GetAllByRoleIdAsync(roleId, cancellationToken);
+            var menuRoleFunctionalityApprovals =
+                await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.GetAllByRoleIdAsync(roleId,
+                    cancellationToken);
 
-                foreach (var menuItem in menuRolesPrivilegeItems)
-                {
-                    var menuRolesPrivilege = menuRolesPrivileges.Where(x => x.Id == menuItem.MenuRolesPrivilegeId).FirstOrDefault();
-                    if (menuRolesPrivilege != null)
-                    {
-                        mapper.Map(menuItem, menuRolesPrivilege);
-                        menuRolesPrivilege.RoleId = roleId;
+            foreach (var menuItem in menuRolesPrivilegeItems)
+            {
+                var menuRolesPrivilege = menuRolesPrivileges.Where(x => x.Id == menuItem.MenuRolesPrivilegeId)
+                    .FirstOrDefault();
+                if (menuRolesPrivilege != null)
+                {
+                    mapper.Map(menuItem, menuRolesPrivilege);
+                    menuRolesPrivilege.RoleId = roleId;
 
-                        await unitOfWork.MenuRolesPrivilegeRepository.UpdateAsync(menuRolesPrivilege, cancellationToken);
-                    }
+                    await unitOfWork.MenuRolesPrivilegeRepository.UpdateAsync(menuRolesPrivilege, cancellationToken);
+                }
 
-                    if (menuItem.MenuRolesFunctionalities.Count > 0)
-                    {
-                        foreach (var menuFunctionality in menuItem.MenuRolesFunctionalities)
-                        {
-                            var functionality = menuRolesFunctionalities.Where(x => x.Id == menuFunctionality.MenuRolesFunctionalityId).FirstOrDefault();
-                            if (functionality != null)
-                            {
-                                mapper.Map(menuFunctionality, functionality);
-                                functionality.RoleId = roleId;
+                if (menuItem.MenuRolesFunctionalities.Count > 0)
+                    foreach (var menuFunctionality in menuItem.MenuRolesFunctionalities)
+                    {
+                        var functionality = menuRolesFunctionalities
+                            .Where(x => x.Id == menuFunctionality.MenuRolesFunctionalityId).FirstOrDefault();
+                        if (functionality != null)
+                        {
+                            mapper.Map(menuFunctionality, functionality);
+                            functionality.RoleId = roleId;
 
-                                await unitOfWork.MenuRolesFunctionalityRepository.UpdateAsync(functionality, cancellationToken);
-                            }
+                            await unitOfWork.MenuRolesFunctionalityRepository.UpdateAsync(functionality,
+                                cancellationToken);
+                        }
 
-                            if (menuFunctionality.MenuRoleFunctionalityApprovalProcess.Count > 0)
+                        if (menuFunctionality.MenuRoleFunctionalityApprovalProcess.Count > 0)
+                            foreach (var applicationFunctionality in menuFunctionality
+                                         .MenuRoleFunctionalityApprovalProcess)
                             {
-                                foreach (var applicationFunctionality in menuFunctionality.MenuRoleFunctionalityApprovalProcess)
-                                {
-                                    var menuRoleFunctionality = menuRoleFunctionalityApprovals.Where(x => x.Id == applicationFunctionality.MenuRoleFunctionalityApprovalProcessId).FirstOrDefault();
-                                    if (menuRoleFunctionality != null)
-                                    {
-                                        mapper.Map(applicationFunctionality, menuRoleFunctionality);
-                                        menuRoleFunctionality.RoleId = roleId;
+                                var menuRoleFunctionality = menuRoleFunctionalityApprovals.Where(x =>
+                                        x.Id == applicationFunctionality.MenuRoleFunctionalityApprovalProcessId)
+                                    .FirstOrDefault();
+                                if (menuRoleFunctionality != null)
+                                {
+                                    mapper.Map(applicationFunctionality, menuRoleFunctionality);
+                                    menuRoleFunctionality.RoleId = roleId;
 
-                                        await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.UpdateAsync(menuRoleFunctionality, cancellationToken);
-                                    }
+                                    await unitOfWork.MenuRoleFunctionalityApprovalProcessRepository.UpdateAsync(
+                                        menuRoleFunctionality, cancellationToken);
                                 }
                             }
-                        }
                     }
-                }
-            }
-            catch (Exception)
-            {
-                await transaction.RollbackAsync(cancellationToken);
+            }
+        }
+        catch (Exception)
+        {
+            await transaction.RollbackAsync(cancellationToken);
 
-                throw;
-            }
-        }
+            throw;
+        }
+    }
 
-        #region Private Methods
-        private static Dictionary<string, Models.Common.Filter> RolesFilters(GetRolesQuery invQuery)
-        {
-            var inboundDto = invQuery.filter;
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    #region Private Methods
+
+    private static Dictionary<string, Models.Common.Filter> RolesFilters(GetRolesQuery invQuery)
+    {
+        var inboundDto = invQuery.filter;
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            if (inboundDto != null)
-            {
-                if (inboundDto?.IsActive != null)
-                    Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? "True" : "False", "IsActive", SearchOperationEnum.Equal);
-
-            }
+        if (inboundDto != null)
+            if (inboundDto?.IsActive != null)
+                Filters.AddFilterIfNotEmpty(filters, inboundDto.IsActive == true ? "True" : "False", "IsActive",
+                    SearchOperationEnum.Equal);
 
-            return filters;
-        }
-        #endregion
-    }
-}
+        return filters;
+    }
+
+    #endregion
+}
\ No newline at end of file
Index: eSusInsurers/Models/Countries/RegionModel.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing eSusInsurers.Common.Mappings;\nusing eSusInsurers.Domain.Entities;\n\nnamespace eSusInsurers.Models.Countries\n{\n    public class RegionModel : IMapFrom<Region>\n    {\n        public int RegionId { get; set; }\n        public string RegionName { get; set; }\n\n        public void Mapping(Profile profile)\n        {\n            profile.CreateMap<Region, RegionModel>()\n                                .ForMember(d => d.RegionId, opt => opt.MapFrom(c => c.Id));\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Models/Countries/RegionModel.cs b/eSusInsurers/Models/Countries/RegionModel.cs
--- a/eSusInsurers/Models/Countries/RegionModel.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Models/Countries/RegionModel.cs	(date 1732264029272)
@@ -2,17 +2,16 @@
 using eSusInsurers.Common.Mappings;
 using eSusInsurers.Domain.Entities;
 
-namespace eSusInsurers.Models.Countries
-{
-    public class RegionModel : IMapFrom<Region>
-    {
-        public int RegionId { get; set; }
-        public string RegionName { get; set; }
+namespace eSusInsurers.Models.Countries;
+
+public class RegionModel : IMapFrom<Region>
+{
+    public int RegionId { get; set; }
+    public string RegionName { get; set; }
 
-        public void Mapping(Profile profile)
-        {
-            profile.CreateMap<Region, RegionModel>()
-                                .ForMember(d => d.RegionId, opt => opt.MapFrom(c => c.Id));
-        }
+    public void Mapping(Profile profile)
+    {
+        profile.CreateMap<Region, RegionModel>()
+            .ForMember(d => d.RegionId, opt => opt.MapFrom(c => c.Id));
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Models/Countries/DistrictModel.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing eSusInsurers.Common.Mappings;\nusing eSusInsurers.Domain.Entities;\n\nnamespace eSusInsurers.Models.Countries\n{\n    public class DistrictModel : IMapFrom<District>\n    {\n        public int DistrictId { get; set; }\n        public string DistrictName { get; set; } = null!;\n        public int RegionId { get; set; }\n\n        public void Mapping(Profile profile)\n        {\n            profile.CreateMap<District, DistrictModel>()\n                                .ForMember(d => d.DistrictId, opt => opt.MapFrom(c => c.Id));\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Models/Countries/DistrictModel.cs b/eSusInsurers/Models/Countries/DistrictModel.cs
--- a/eSusInsurers/Models/Countries/DistrictModel.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Models/Countries/DistrictModel.cs	(date 1732264029270)
@@ -2,18 +2,17 @@
 using eSusInsurers.Common.Mappings;
 using eSusInsurers.Domain.Entities;
 
-namespace eSusInsurers.Models.Countries
-{
-    public class DistrictModel : IMapFrom<District>
-    {
-        public int DistrictId { get; set; }
-        public string DistrictName { get; set; } = null!;
-        public int RegionId { get; set; }
+namespace eSusInsurers.Models.Countries;
+
+public class DistrictModel : IMapFrom<District>
+{
+    public int DistrictId { get; set; }
+    public string DistrictName { get; set; } = null!;
+    public int RegionId { get; set; }
 
-        public void Mapping(Profile profile)
-        {
-            profile.CreateMap<District, DistrictModel>()
-                                .ForMember(d => d.DistrictId, opt => opt.MapFrom(c => c.Id));
-        }
+    public void Mapping(Profile profile)
+    {
+        profile.CreateMap<District, DistrictModel>()
+            .ForMember(d => d.DistrictId, opt => opt.MapFrom(c => c.Id));
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Middleware/TokenRefreshMiddleware.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using eSusInsurers.Common.Exceptions;\nusing eSusInsurers.Services.Interfaces;\nusing System.IdentityModel.Tokens.Jwt;\n\nnamespace eSusInsurers.Middleware\n{\n    /// <summary>\n    /// Middleware logic for refreshing the token.\n    /// </summary>\n    public class TokenRefreshMiddleware(IUserService userService, IHttpContextAccessor httpContextAccessor) : IMiddleware\n    {\n        /// <summary>\n        /// Method to read the token and update.\n        /// </summary>\n        /// <param name=\"context\"></param>\n        /// <param name=\"next\"></param>\n        /// <returns></returns>\n        public async Task InvokeAsync(HttpContext context, RequestDelegate next)\n        {\n            try\n            {\n                var token = GetTokenFromRequest(context);\n\n                if (!string.IsNullOrEmpty(token))\n                {\n                    bool tokenNeedsRefresh = TokenNeedsRefresh(context);\n\n                    if (tokenNeedsRefresh)\n                    {\n                        // Call the token service to refresh the token\n                        //var response = await userService.RefreshTokenAsync(token, httpContextAccessor, new CancellationToken());\n\n                        // Set the refreshed token in the response header\n                        context.Response.Headers.Append(\"Access-Control-Expose-Headers\", \"Authorization\");\n                        context.Response.Headers.Append(\"Access-Control-Expose-Headers\", \"X-Token-Refreshed\");\n                        //context.Response.Headers.Append(\"Authorization\", response.Token);\n                        context.Response.Headers.Append(\"X-Token-Refreshed\", true.ToString());\n\n                    }\n                }\n            }\n            catch (UnauthorizedException)\n            {\n                context.Response.StatusCode = StatusCodes.Status401Unauthorized;\n                context.Response.ContentType = \"text/plain\";\n                await context.Response.WriteAsync(\"Unauthorized: Invalid or expired token.\");\n                return;\n            }\n\n            await next(context);\n\n        }\n\n        private bool TokenNeedsRefresh(HttpContext context)\n        {\n            // Get the current token and its expiration time from the request\n            string currentToken = GetTokenFromRequest(context);\n            DateTime expirationTime = GetTokenExpirationTime(currentToken);\n\n            // Check if the token expiration is within 5 minutes from now\n            TimeSpan remainingValidity = expirationTime - DateTime.UtcNow;\n\n            if (remainingValidity.TotalSeconds <= 0)\n            {\n                throw new UnauthorizedException();\n            }\n\n            return remainingValidity.TotalMinutes < 60;\n        }\n\n        private string GetTokenFromRequest(HttpContext context)\n        {\n            return context.Request.Headers[\"Authorization\"].ToString().Replace(\"Bearer \", \"\");\n        }\n\n        private DateTime GetTokenExpirationTime(string token)\n        {\n            var handler = new JwtSecurityTokenHandler();\n            var tokens = handler.ReadToken(token) as JwtSecurityToken;\n            DateTime expirationTime = tokens.ValidTo;\n            return expirationTime;\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Middleware/TokenRefreshMiddleware.cs b/eSusInsurers/Middleware/TokenRefreshMiddleware.cs
--- a/eSusInsurers/Middleware/TokenRefreshMiddleware.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Middleware/TokenRefreshMiddleware.cs	(date 1732264029111)
@@ -1,84 +1,78 @@
+using System.IdentityModel.Tokens.Jwt;
 using eSusInsurers.Common.Exceptions;
 using eSusInsurers.Services.Interfaces;
-using System.IdentityModel.Tokens.Jwt;
 
-namespace eSusInsurers.Middleware
-{
-    /// <summary>
-    /// Middleware logic for refreshing the token.
-    /// </summary>
-    public class TokenRefreshMiddleware(IUserService userService, IHttpContextAccessor httpContextAccessor) : IMiddleware
-    {
-        /// <summary>
-        /// Method to read the token and update.
-        /// </summary>
-        /// <param name="context"></param>
-        /// <param name="next"></param>
-        /// <returns></returns>
-        public async Task InvokeAsync(HttpContext context, RequestDelegate next)
-        {
-            try
-            {
-                var token = GetTokenFromRequest(context);
+namespace eSusInsurers.Middleware;
+
+/// <summary>
+///     Middleware logic for refreshing the token.
+/// </summary>
+public class TokenRefreshMiddleware(IUserService userService, IHttpContextAccessor httpContextAccessor) : IMiddleware
+{
+    /// <summary>
+    ///     Method to read the token and update.
+    /// </summary>
+    /// <param name="context"></param>
+    /// <param name="next"></param>
+    /// <returns></returns>
+    public async Task InvokeAsync(HttpContext context, RequestDelegate next)
+    {
+        try
+        {
+            var token = GetTokenFromRequest(context);
 
-                if (!string.IsNullOrEmpty(token))
-                {
-                    bool tokenNeedsRefresh = TokenNeedsRefresh(context);
+            if (!string.IsNullOrEmpty(token))
+            {
+                var tokenNeedsRefresh = TokenNeedsRefresh(context);
 
-                    if (tokenNeedsRefresh)
-                    {
-                        // Call the token service to refresh the token
-                        //var response = await userService.RefreshTokenAsync(token, httpContextAccessor, new CancellationToken());
+                if (tokenNeedsRefresh)
+                {
+                    // Call the token service to refresh the token
+                    //var response = await userService.RefreshTokenAsync(token, httpContextAccessor, new CancellationToken());
 
-                        // Set the refreshed token in the response header
-                        context.Response.Headers.Append("Access-Control-Expose-Headers", "Authorization");
-                        context.Response.Headers.Append("Access-Control-Expose-Headers", "X-Token-Refreshed");
-                        //context.Response.Headers.Append("Authorization", response.Token);
-                        context.Response.Headers.Append("X-Token-Refreshed", true.ToString());
-
-                    }
-                }
-            }
-            catch (UnauthorizedException)
-            {
-                context.Response.StatusCode = StatusCodes.Status401Unauthorized;
-                context.Response.ContentType = "text/plain";
-                await context.Response.WriteAsync("Unauthorized: Invalid or expired token.");
-                return;
-            }
+                    // Set the refreshed token in the response header
+                    context.Response.Headers.Append("Access-Control-Expose-Headers", "Authorization");
+                    context.Response.Headers.Append("Access-Control-Expose-Headers", "X-Token-Refreshed");
+                    //context.Response.Headers.Append("Authorization", response.Token);
+                    context.Response.Headers.Append("X-Token-Refreshed", true.ToString());
+                }
+            }
+        }
+        catch (UnauthorizedException)
+        {
+            context.Response.StatusCode = StatusCodes.Status401Unauthorized;
+            context.Response.ContentType = "text/plain";
+            await context.Response.WriteAsync("Unauthorized: Invalid or expired token.");
+            return;
+        }
 
-            await next(context);
-
-        }
+        await next(context);
+    }
 
-        private bool TokenNeedsRefresh(HttpContext context)
-        {
-            // Get the current token and its expiration time from the request
-            string currentToken = GetTokenFromRequest(context);
-            DateTime expirationTime = GetTokenExpirationTime(currentToken);
+    private bool TokenNeedsRefresh(HttpContext context)
+    {
+        // Get the current token and its expiration time from the request
+        var currentToken = GetTokenFromRequest(context);
+        var expirationTime = GetTokenExpirationTime(currentToken);
 
-            // Check if the token expiration is within 5 minutes from now
-            TimeSpan remainingValidity = expirationTime - DateTime.UtcNow;
+        // Check if the token expiration is within 5 minutes from now
+        var remainingValidity = expirationTime - DateTime.UtcNow;
 
-            if (remainingValidity.TotalSeconds <= 0)
-            {
-                throw new UnauthorizedException();
-            }
+        if (remainingValidity.TotalSeconds <= 0) throw new UnauthorizedException();
 
-            return remainingValidity.TotalMinutes < 60;
-        }
+        return remainingValidity.TotalMinutes < 60;
+    }
 
-        private string GetTokenFromRequest(HttpContext context)
-        {
-            return context.Request.Headers["Authorization"].ToString().Replace("Bearer ", "");
-        }
+    private string GetTokenFromRequest(HttpContext context)
+    {
+        return context.Request.Headers["Authorization"].ToString().Replace("Bearer ", "");
+    }
 
-        private DateTime GetTokenExpirationTime(string token)
-        {
-            var handler = new JwtSecurityTokenHandler();
-            var tokens = handler.ReadToken(token) as JwtSecurityToken;
-            DateTime expirationTime = tokens.ValidTo;
-            return expirationTime;
-        }
+    private DateTime GetTokenExpirationTime(string token)
+    {
+        var handler = new JwtSecurityTokenHandler();
+        var tokens = handler.ReadToken(token) as JwtSecurityToken;
+        var expirationTime = tokens.ValidTo;
+        return expirationTime;
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Services/Common/Filters.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using eSusInsurers.Models.enums;\nusing eSusInsurers.Models.Common;\n\nnamespace eSusInsurers.Services.Common\n{\n    public class Filters\n    {\n        public static void AddFilterIfNotEmpty(Dictionary<string, Models.Common.Filter> filters, string? value, string fieldName, SearchOperationEnum operation, bool useOr = false)\n        {\n            if (!string.IsNullOrEmpty(value))\n            {\n                filters.Add(fieldName, CreateFilter(fieldName, value, operation, useOr));\n            }\n        }\n\n        public static void AddFilterIfValueGreaterThanZero(Dictionary<string, Models.Common.Filter> filters, long? value, string fieldName, SearchOperationEnum operation, bool useOr = false)\n        {\n            if (value.HasValue && value.Value > 0)\n            {\n                filters.Add(fieldName, CreateFilter(fieldName, value.Value.ToString(), operation, useOr));\n            }\n        }\n\n        public static Models.Common.Filter CreateFilter(string fieldName, string value, SearchOperationEnum operation, bool useOrLogic = false)\n        {\n            return new Models.Common.Filter\n            {\n                FieldName = fieldName,\n                Value = value,\n                Operation = operation,\n                UseOrLogic = useOrLogic\n            };\n        }\n\n        internal static void AddFilterIfNotEmpty(Dictionary<string, Models.Common.Filter> filters, int cropCategoryId, string v, SearchOperationEnum equal)\n        {\n            throw new NotImplementedException();\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Common/Filters.cs b/eSusInsurers/Services/Common/Filters.cs
--- a/eSusInsurers/Services/Common/Filters.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Common/Filters.cs	(date 1732264029209)
@@ -1,40 +1,37 @@
 using eSusInsurers.Models.enums;
-using eSusInsurers.Models.Common;
 
-namespace eSusInsurers.Services.Common
-{
-    public class Filters
-    {
-        public static void AddFilterIfNotEmpty(Dictionary<string, Models.Common.Filter> filters, string? value, string fieldName, SearchOperationEnum operation, bool useOr = false)
-        {
-            if (!string.IsNullOrEmpty(value))
-            {
-                filters.Add(fieldName, CreateFilter(fieldName, value, operation, useOr));
-            }
-        }
+namespace eSusInsurers.Services.Common;
+
+public class Filters
+{
+    public static void AddFilterIfNotEmpty(Dictionary<string, Models.Common.Filter> filters, string? value,
+        string fieldName, SearchOperationEnum operation, bool useOr = false)
+    {
+        if (!string.IsNullOrEmpty(value)) filters.Add(fieldName, CreateFilter(fieldName, value, operation, useOr));
+    }
 
-        public static void AddFilterIfValueGreaterThanZero(Dictionary<string, Models.Common.Filter> filters, long? value, string fieldName, SearchOperationEnum operation, bool useOr = false)
-        {
-            if (value.HasValue && value.Value > 0)
-            {
-                filters.Add(fieldName, CreateFilter(fieldName, value.Value.ToString(), operation, useOr));
-            }
-        }
+    public static void AddFilterIfValueGreaterThanZero(Dictionary<string, Models.Common.Filter> filters, long? value,
+        string fieldName, SearchOperationEnum operation, bool useOr = false)
+    {
+        if (value.HasValue && value.Value > 0)
+            filters.Add(fieldName, CreateFilter(fieldName, value.Value.ToString(), operation, useOr));
+    }
 
-        public static Models.Common.Filter CreateFilter(string fieldName, string value, SearchOperationEnum operation, bool useOrLogic = false)
-        {
-            return new Models.Common.Filter
-            {
-                FieldName = fieldName,
-                Value = value,
-                Operation = operation,
-                UseOrLogic = useOrLogic
-            };
-        }
+    public static Models.Common.Filter CreateFilter(string fieldName, string value, SearchOperationEnum operation,
+        bool useOrLogic = false)
+    {
+        return new Models.Common.Filter
+        {
+            FieldName = fieldName,
+            Value = value,
+            Operation = operation,
+            UseOrLogic = useOrLogic
+        };
+    }
 
-        internal static void AddFilterIfNotEmpty(Dictionary<string, Models.Common.Filter> filters, int cropCategoryId, string v, SearchOperationEnum equal)
-        {
-            throw new NotImplementedException();
-        }
+    internal static void AddFilterIfNotEmpty(Dictionary<string, Models.Common.Filter> filters, int cropCategoryId,
+        string v, SearchOperationEnum equal)
+    {
+        throw new NotImplementedException();
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Services/Interfaces/ICountriesService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using eSusInsurers.Models.Countries;\nusing eSusInsurers.Models.Users.GetUsers;\n\nnamespace eSusInsurers.Services.Interfaces\n{\n    public interface ICountriesService\n    {\n        Task<List<RegionModel>?> GetRegions(long countryId, CancellationToken cancellationToken);\n        Task<List<DistrictModel>?> GetDistricts(long countryId, long regionId, CancellationToken cancellationToken);\n        Task<List<SubCountiesModel>?> GetSubcounties(long countryId, long regionId, long districtId, CancellationToken cancellationToken);\n        Task<List<ParishModel>?> GetParishes(long countryId, long regionId, long districtId, long subCountyId, CancellationToken cancellationToken);\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Interfaces/ICountriesService.cs b/eSusInsurers/Services/Interfaces/ICountriesService.cs
--- a/eSusInsurers/Services/Interfaces/ICountriesService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Interfaces/ICountriesService.cs	(date 1732264029133)
@@ -1,13 +1,15 @@
 using eSusInsurers.Models.Countries;
-using eSusInsurers.Models.Users.GetUsers;
 
-namespace eSusInsurers.Services.Interfaces
-{
-    public interface ICountriesService
-    {
-        Task<List<RegionModel>?> GetRegions(long countryId, CancellationToken cancellationToken);
-        Task<List<DistrictModel>?> GetDistricts(long countryId, long regionId, CancellationToken cancellationToken);
-        Task<List<SubCountiesModel>?> GetSubcounties(long countryId, long regionId, long districtId, CancellationToken cancellationToken);
-        Task<List<ParishModel>?> GetParishes(long countryId, long regionId, long districtId, long subCountyId, CancellationToken cancellationToken);
-    }
-}
+namespace eSusInsurers.Services.Interfaces;
+
+public interface ICountriesService
+{
+    Task<List<RegionModel>?> GetRegions(long countryId, CancellationToken cancellationToken);
+    Task<List<DistrictModel>?> GetDistricts(long countryId, long regionId, CancellationToken cancellationToken);
+
+    Task<List<SubCountiesModel>?> GetSubcounties(long countryId, long regionId, long districtId,
+        CancellationToken cancellationToken);
+
+    Task<List<ParishModel>?> GetParishes(long countryId, long regionId, long districtId, long subCountyId,
+        CancellationToken cancellationToken);
+}
\ No newline at end of file
Index: eSusInsurers/Controllers/CountriesController.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using eSusInsurers.Models.Countries;\nusing eSusInsurers.Services.Interfaces;\nusing Microsoft.AspNetCore.Authorization;\nusing Microsoft.AspNetCore.Mvc;\n\nnamespace eSusInsurers.Controllers\n{\n    /// <summary>\n    /// Controller for countries.\n    /// </summary>\n    [Route(\"countries\")]\n    public class CountriesController(ICountriesService countriesService) : BaseController\n    {\n        /// <summary>\n        /// Get regions by country Id\n        /// </summary>\n        /// <remarks>\n        /// Returns regions.\n        /// </remarks>\n        /// <param name=\"countryId\">Country Id.</param>\n        /// <response code=\"200\">Returns regions.</response>\n        /// <returns>Returns regions</returns>\n        [HttpGet(\"{countryId}/regions\"), Authorize]\n        [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<RegionModel>))]\n        public async Task<ActionResult<List<RegionModel>>> GetRegions(long countryId)\n        {\n            try\n            {\n                var result = await countriesService.GetRegions(countryId, new CancellationToken());\n\n                return Ok(result);\n            }\n            catch (Exception e)\n            {\n                return BadRequest(new { ErrorMessage = e.Message });\n            }\n        }\n\n        /// <summary>\n        /// Get districts by region id\n        /// </summary>\n        /// <remarks>\n        /// Returns districts.\n        /// </remarks>\n        /// <param name=\"regionId\">Region Id</param>\n        /// <param name=\"countryId\">Country Id</param>\n        /// <response code=\"200\">Returns districts.</response>\n        /// <returns>Returns districts</returns>\n        [HttpGet(\"{countryId}/regions/{regionId}/districts\"), Authorize]\n        [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<DistrictModel>))]\n        public async Task<ActionResult<List<DistrictModel>>> GetDistricts(long countryId, long regionId)\n        {\n            try\n            {\n                var result = await countriesService.GetDistricts(countryId, regionId, new CancellationToken());\n                return Ok(result);\n            }\n            catch (Exception e)\n            {\n                return BadRequest(new { ErrorMessage = e.Message });\n            }\n        }\n\n        /// <summary>\n        /// Get subcounties by district id\n        /// </summary>\n        /// <remarks>\n        /// Returns subcounties.\n        /// </remarks>\n        /// <param name=\"regionId\">Region Id</param>\n        /// <param name=\"countryId\">Country Id</param>\n        /// <param name=\"districtId\">District Id</param>\n        /// <response code=\"200\">Returns subcounties.</response>\n        /// <returns>Returns subcounties</returns>\n        [HttpGet(\"{countryId}/regions/{regionId}/districts/{districtId}/subcounties\"), Authorize]\n        [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<SubCountiesModel>))]\n        public async Task<ActionResult<List<SubCountiesModel>>> GetSubcounties(long countryId, long regionId, long districtId)\n        {\n            try\n            {\n                var result = await countriesService.GetSubcounties(countryId, regionId, districtId, new CancellationToken());\n                return Ok(result);\n            }\n            catch (Exception e)\n            {\n                return BadRequest(new { ErrorMessage = e.Message });\n            }\n        }\n\n        /// <summary>\n        /// Get parishes by subcounty id\n        /// </summary>\n        /// <remarks>\n        /// Returns parishes.\n        /// </remarks>\n        /// <param name=\"regionId\">Region Id</param>\n        /// <param name=\"countryId\">Country Id</param>\n        /// <param name=\"districtId\">District Id</param>\n        /// <param name=\"subcountyId\">Subcounty Id</param>\n        /// <response code=\"200\">Returns parishes.</response>\n        /// <returns>Returns parishes</returns>\n        [HttpGet(\"{countryId}/regions/{regionId}/districts/{districtId}/subcounties/{subcountyId}/parishes\"), Authorize]\n        [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<ParishModel>))]\n        public async Task<ActionResult<List<ParishModel>>> GetParishes(long countryId, long regionId, long districtId, long subcountyId)\n        {\n            try\n            {\n                var result = await countriesService.GetParishes(countryId, regionId, districtId, subcountyId, new CancellationToken());\n                return Ok(result);\n            }\n            catch (Exception e)\n            {\n                return BadRequest(new { ErrorMessage = e.Message });\n            }\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Controllers/CountriesController.cs b/eSusInsurers/Controllers/CountriesController.cs
--- a/eSusInsurers/Controllers/CountriesController.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Controllers/CountriesController.cs	(date 1732264029107)
@@ -3,115 +3,122 @@
 using Microsoft.AspNetCore.Authorization;
 using Microsoft.AspNetCore.Mvc;
 
-namespace eSusInsurers.Controllers
-{
-    /// <summary>
-    /// Controller for countries.
-    /// </summary>
-    [Route("countries")]
-    public class CountriesController(ICountriesService countriesService) : BaseController
-    {
-        /// <summary>
-        /// Get regions by country Id
-        /// </summary>
-        /// <remarks>
-        /// Returns regions.
-        /// </remarks>
-        /// <param name="countryId">Country Id.</param>
-        /// <response code="200">Returns regions.</response>
-        /// <returns>Returns regions</returns>
-        [HttpGet("{countryId}/regions"), Authorize]
-        [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<RegionModel>))]
-        public async Task<ActionResult<List<RegionModel>>> GetRegions(long countryId)
-        {
-            try
-            {
-                var result = await countriesService.GetRegions(countryId, new CancellationToken());
+namespace eSusInsurers.Controllers;
+
+/// <summary>
+///     Controller for countries.
+/// </summary>
+[Route("countries")]
+public class CountriesController(ICountriesService countriesService) : BaseController
+{
+    /// <summary>
+    ///     Get regions by country Id
+    /// </summary>
+    /// <remarks>
+    ///     Returns regions.
+    /// </remarks>
+    /// <param name="countryId">Country Id.</param>
+    /// <response code="200">Returns regions.</response>
+    /// <returns>Returns regions</returns>
+    [HttpGet("{countryId}/regions")]
+    [Authorize]
+    [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<RegionModel>))]
+    public async Task<ActionResult<List<RegionModel>>> GetRegions(long countryId)
+    {
+        try
+        {
+            var result = await countriesService.GetRegions(countryId, new CancellationToken());
 
-                return Ok(result);
-            }
-            catch (Exception e)
-            {
-                return BadRequest(new { ErrorMessage = e.Message });
-            }
-        }
+            return Ok(result);
+        }
+        catch (Exception e)
+        {
+            return BadRequest(new { ErrorMessage = e.Message });
+        }
+    }
 
-        /// <summary>
-        /// Get districts by region id
-        /// </summary>
-        /// <remarks>
-        /// Returns districts.
-        /// </remarks>
-        /// <param name="regionId">Region Id</param>
-        /// <param name="countryId">Country Id</param>
-        /// <response code="200">Returns districts.</response>
-        /// <returns>Returns districts</returns>
-        [HttpGet("{countryId}/regions/{regionId}/districts"), Authorize]
-        [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<DistrictModel>))]
-        public async Task<ActionResult<List<DistrictModel>>> GetDistricts(long countryId, long regionId)
-        {
-            try
-            {
-                var result = await countriesService.GetDistricts(countryId, regionId, new CancellationToken());
-                return Ok(result);
-            }
-            catch (Exception e)
-            {
-                return BadRequest(new { ErrorMessage = e.Message });
-            }
-        }
+    /// <summary>
+    ///     Get districts by region id
+    /// </summary>
+    /// <remarks>
+    ///     Returns districts.
+    /// </remarks>
+    /// <param name="regionId">Region Id</param>
+    /// <param name="countryId">Country Id</param>
+    /// <response code="200">Returns districts.</response>
+    /// <returns>Returns districts</returns>
+    [HttpGet("{countryId}/regions/{regionId}/districts")]
+    [Authorize]
+    [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<DistrictModel>))]
+    public async Task<ActionResult<List<DistrictModel>>> GetDistricts(long countryId, long regionId)
+    {
+        try
+        {
+            var result = await countriesService.GetDistricts(countryId, regionId, new CancellationToken());
+            return Ok(result);
+        }
+        catch (Exception e)
+        {
+            return BadRequest(new { ErrorMessage = e.Message });
+        }
+    }
 
-        /// <summary>
-        /// Get subcounties by district id
-        /// </summary>
-        /// <remarks>
-        /// Returns subcounties.
-        /// </remarks>
-        /// <param name="regionId">Region Id</param>
-        /// <param name="countryId">Country Id</param>
-        /// <param name="districtId">District Id</param>
-        /// <response code="200">Returns subcounties.</response>
-        /// <returns>Returns subcounties</returns>
-        [HttpGet("{countryId}/regions/{regionId}/districts/{districtId}/subcounties"), Authorize]
-        [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<SubCountiesModel>))]
-        public async Task<ActionResult<List<SubCountiesModel>>> GetSubcounties(long countryId, long regionId, long districtId)
-        {
-            try
-            {
-                var result = await countriesService.GetSubcounties(countryId, regionId, districtId, new CancellationToken());
-                return Ok(result);
-            }
-            catch (Exception e)
-            {
-                return BadRequest(new { ErrorMessage = e.Message });
-            }
-        }
+    /// <summary>
+    ///     Get subcounties by district id
+    /// </summary>
+    /// <remarks>
+    ///     Returns subcounties.
+    /// </remarks>
+    /// <param name="regionId">Region Id</param>
+    /// <param name="countryId">Country Id</param>
+    /// <param name="districtId">District Id</param>
+    /// <response code="200">Returns subcounties.</response>
+    /// <returns>Returns subcounties</returns>
+    [HttpGet("{countryId}/regions/{regionId}/districts/{districtId}/subcounties")]
+    [Authorize]
+    [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<SubCountiesModel>))]
+    public async Task<ActionResult<List<SubCountiesModel>>> GetSubcounties(long countryId, long regionId,
+        long districtId)
+    {
+        try
+        {
+            var result =
+                await countriesService.GetSubcounties(countryId, regionId, districtId, new CancellationToken());
+            return Ok(result);
+        }
+        catch (Exception e)
+        {
+            return BadRequest(new { ErrorMessage = e.Message });
+        }
+    }
 
-        /// <summary>
-        /// Get parishes by subcounty id
-        /// </summary>
-        /// <remarks>
-        /// Returns parishes.
-        /// </remarks>
-        /// <param name="regionId">Region Id</param>
-        /// <param name="countryId">Country Id</param>
-        /// <param name="districtId">District Id</param>
-        /// <param name="subcountyId">Subcounty Id</param>
-        /// <response code="200">Returns parishes.</response>
-        /// <returns>Returns parishes</returns>
-        [HttpGet("{countryId}/regions/{regionId}/districts/{districtId}/subcounties/{subcountyId}/parishes"), Authorize]
-        [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<ParishModel>))]
-        public async Task<ActionResult<List<ParishModel>>> GetParishes(long countryId, long regionId, long districtId, long subcountyId)
-        {
-            try
-            {
-                var result = await countriesService.GetParishes(countryId, regionId, districtId, subcountyId, new CancellationToken());
-                return Ok(result);
-            }
-            catch (Exception e)
-            {
-                return BadRequest(new { ErrorMessage = e.Message });
-            }
+    /// <summary>
+    ///     Get parishes by subcounty id
+    /// </summary>
+    /// <remarks>
+    ///     Returns parishes.
+    /// </remarks>
+    /// <param name="regionId">Region Id</param>
+    /// <param name="countryId">Country Id</param>
+    /// <param name="districtId">District Id</param>
+    /// <param name="subcountyId">Subcounty Id</param>
+    /// <response code="200">Returns parishes.</response>
+    /// <returns>Returns parishes</returns>
+    [HttpGet("{countryId}/regions/{regionId}/districts/{districtId}/subcounties/{subcountyId}/parishes")]
+    [Authorize]
+    [ProducesResponseType(StatusCodes.Status200OK, Type = typeof(List<ParishModel>))]
+    public async Task<ActionResult<List<ParishModel>>> GetParishes(long countryId, long regionId, long districtId,
+        long subcountyId)
+    {
+        try
+        {
+            var result = await countriesService.GetParishes(countryId, regionId, districtId, subcountyId,
+                new CancellationToken());
+            return Ok(result);
+        }
+        catch (Exception e)
+        {
+            return BadRequest(new { ErrorMessage = e.Message });
         }
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/InsuranceProviderService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing AzureIntegrations.API.Interfaces;\nusing AzureIntegrations.API.Models;\nusing eSusFarmInternal.API.Interfaces;\nusing eSusInsurers.Common.Logging;\nusing eSusInsurers.Constants;\nusing eSusInsurers.Domain.Entities;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Models.InsuranceProviders.CreateInsuranceProvider;\nusing eSusInsurers.Services.Interfaces;\n\nnamespace eSusInsurers.Services.Implementations\n{\n    public class InsuranceProviderService : IInsuranceProviderService\n    {\n        public IUnitOfWork _unitOfWork;\n        private readonly IMapper _mapper;\n        private readonly IInternaleSusFarmService _internaleSusFarmService;\n        private readonly IAzureFileStorageConnection _azureFileStorageConnection;\n        private readonly ILoggerContext<InsuranceProvider> _logger;\n\n\n\n        public InsuranceProviderService(IUnitOfWork unitOfWork\n                                      , IMapper mapper\n                                      , IAzureFileStorageConnection azureFileStorageConnection\n                                      , ILoggerContext<InsuranceProvider> logger\n                                      , IInternaleSusFarmService internaleSusFarmService)\n        {\n            _unitOfWork = unitOfWork;\n            _mapper = mapper;\n            _azureFileStorageConnection = azureFileStorageConnection;\n            _logger = logger;\n            _internaleSusFarmService = internaleSusFarmService;\n        }\n\n        public async Task<CreateInsuranceProviderResponse> CreateInsuranceProvider(CreateInsuranceProviderRequest request, CancellationToken cancellationToken)\n        {\n            ArgumentNullException.ThrowIfNull(request, nameof(request));\n\n            // var getprovinces = await _internaleSusFarmService.GetProvinceSummary(cancellationToken);\n\n            var insuranceProvider = _mapper.Map<InsuranceProvider>(request);\n\n            insuranceProvider.CreatedDate = DateTime.UtcNow;\n\n            insuranceProvider.ModifiedDate = DateTime.UtcNow;\n\n            using var transaction = _unitOfWork.BeginTransaction();\n\n            try\n            {\n\n                await _unitOfWork.InsuranceProviderRepository.AddAsync(insuranceProvider, cancellationToken);\n\n                await _unitOfWork.SaveChangesAsync(cancellationToken);\n\n                if (request.InsuranceProviderFiles?.Count() > 0)\n                {\n                    var insuranceProviderDocuments = await UploadInsuranceProviderFiles(request.InsuranceProviderFiles.ToList(), insuranceProvider);\n\n                    if (insuranceProviderDocuments.Count > 0)\n                    {\n                        await _unitOfWork.InsuranceProviderDocumentRepository.AddRangeAsync(insuranceProviderDocuments, cancellationToken);\n\n                        await _unitOfWork.SaveChangesAsync(cancellationToken);\n                    }\n                }\n\n                await transaction.CommitAsync(cancellationToken);\n\n                return new CreateInsuranceProviderResponse()\n                {\n                    InsuranceProviderId = insuranceProvider.Id,\n                    Success = true\n                };\n            }\n            catch (Exception ex)\n            {\n                await _logger.LogErrorAsync(insuranceProvider, ex, ApplicationConstants.CommorError, cancellationToken);\n\n                await transaction.RollbackAsync(cancellationToken);\n\n                return new CreateInsuranceProviderResponse()\n                {\n                    Success = false,\n                    FailureReason = ex.Message\n                };\n            }\n        }\n\n        private async Task<List<InsuranceProviderDocument>> UploadInsuranceProviderFiles(List<InsuranceProviderFiles> InsuranceProviderFiles, InsuranceProvider insuranceProvider)\n        {\n\n            List<InsuranceProviderDocument> insuranceProviderDocuments = new List<InsuranceProviderDocument>();\n\n            try\n            {\n\n                foreach (var file in InsuranceProviderFiles)\n                {\n                    if (file.DocumentData != null)\n                    {\n                        AzureDocument document = new AzureDocument();\n                        document.DocumentName = $\"{insuranceProvider.Id}_{file.DocumentName}\";\n                        document.DocumentData = Convert.FromBase64String(file.DocumentData);\n                        document.MainDirectory = ApplicationConstants.AzureMainDirectory;\n                        document.ChildDirectory = insuranceProvider.Id.ToString();\n\n                        var documentPath = await _azureFileStorageConnection.UploadInsuranceProviderFiles(document);\n\n                        if (documentPath != null)\n                        {\n                            InsuranceProviderDocument insuranceProviderDocument = new InsuranceProviderDocument();\n                            insuranceProviderDocument.InsurerId = insuranceProvider.Id;\n                            insuranceProviderDocument.DocumentName = file.DocumentName;\n                            insuranceProviderDocument.DocumentPath = documentPath;\n                            insuranceProviderDocument.IsActive = true;\n                            insuranceProviderDocument.CreatedBy = insuranceProviderDocument.ModifiedBy = insuranceProvider.InsurerName;\n                            insuranceProviderDocument.CreatedDate = DateTime.UtcNow;\n                            insuranceProviderDocument.ModifiedDate = DateTime.UtcNow;\n\n                            insuranceProviderDocuments.Add(insuranceProviderDocument);\n                        }\n\n                    }\n                }\n            }\n            catch (Exception ex)\n            {\n                throw;\n            }\n\n            return insuranceProviderDocuments;\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/InsuranceProviderService.cs b/eSusInsurers/Services/Implementations/InsuranceProviderService.cs
--- a/eSusInsurers/Services/Implementations/InsuranceProviderService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/InsuranceProviderService.cs	(date 1732264029113)
@@ -9,129 +9,119 @@
 using eSusInsurers.Models.InsuranceProviders.CreateInsuranceProvider;
 using eSusInsurers.Services.Interfaces;
 
-namespace eSusInsurers.Services.Implementations
-{
-    public class InsuranceProviderService : IInsuranceProviderService
-    {
-        public IUnitOfWork _unitOfWork;
-        private readonly IMapper _mapper;
-        private readonly IInternaleSusFarmService _internaleSusFarmService;
-        private readonly IAzureFileStorageConnection _azureFileStorageConnection;
-        private readonly ILoggerContext<InsuranceProvider> _logger;
-
+namespace eSusInsurers.Services.Implementations;
+
+public class InsuranceProviderService : IInsuranceProviderService
+{
+    private readonly IAzureFileStorageConnection _azureFileStorageConnection;
+    private readonly IInternaleSusFarmService _internaleSusFarmService;
+    private readonly ILoggerContext<InsuranceProvider> _logger;
+    private readonly IMapper _mapper;
+    public IUnitOfWork _unitOfWork;
 
 
-        public InsuranceProviderService(IUnitOfWork unitOfWork
-                                      , IMapper mapper
-                                      , IAzureFileStorageConnection azureFileStorageConnection
-                                      , ILoggerContext<InsuranceProvider> logger
-                                      , IInternaleSusFarmService internaleSusFarmService)
-        {
-            _unitOfWork = unitOfWork;
-            _mapper = mapper;
-            _azureFileStorageConnection = azureFileStorageConnection;
-            _logger = logger;
-            _internaleSusFarmService = internaleSusFarmService;
-        }
+    public InsuranceProviderService(IUnitOfWork unitOfWork
+        , IMapper mapper
+        , IAzureFileStorageConnection azureFileStorageConnection
+        , ILoggerContext<InsuranceProvider> logger
+        , IInternaleSusFarmService internaleSusFarmService)
+    {
+        _unitOfWork = unitOfWork;
+        _mapper = mapper;
+        _azureFileStorageConnection = azureFileStorageConnection;
+        _logger = logger;
+        _internaleSusFarmService = internaleSusFarmService;
+    }
 
-        public async Task<CreateInsuranceProviderResponse> CreateInsuranceProvider(CreateInsuranceProviderRequest request, CancellationToken cancellationToken)
-        {
-            ArgumentNullException.ThrowIfNull(request, nameof(request));
+    public async Task<CreateInsuranceProviderResponse> CreateInsuranceProvider(CreateInsuranceProviderRequest request,
+        CancellationToken cancellationToken)
+    {
+        ArgumentNullException.ThrowIfNull(request, nameof(request));
 
-            // var getprovinces = await _internaleSusFarmService.GetProvinceSummary(cancellationToken);
+        // var getprovinces = await _internaleSusFarmService.GetProvinceSummary(cancellationToken);
 
-            var insuranceProvider = _mapper.Map<InsuranceProvider>(request);
+        var insuranceProvider = _mapper.Map<InsuranceProvider>(request);
 
-            insuranceProvider.CreatedDate = DateTime.UtcNow;
+        insuranceProvider.CreatedDate = DateTime.UtcNow;
 
-            insuranceProvider.ModifiedDate = DateTime.UtcNow;
+        insuranceProvider.ModifiedDate = DateTime.UtcNow;
 
-            using var transaction = _unitOfWork.BeginTransaction();
+        using var transaction = _unitOfWork.BeginTransaction();
 
-            try
-            {
-
-                await _unitOfWork.InsuranceProviderRepository.AddAsync(insuranceProvider, cancellationToken);
+        try
+        {
+            await _unitOfWork.InsuranceProviderRepository.AddAsync(insuranceProvider, cancellationToken);
 
-                await _unitOfWork.SaveChangesAsync(cancellationToken);
+            await _unitOfWork.SaveChangesAsync(cancellationToken);
 
-                if (request.InsuranceProviderFiles?.Count() > 0)
-                {
-                    var insuranceProviderDocuments = await UploadInsuranceProviderFiles(request.InsuranceProviderFiles.ToList(), insuranceProvider);
+            if (request.InsuranceProviderFiles?.Count() > 0)
+            {
+                var insuranceProviderDocuments =
+                    await UploadInsuranceProviderFiles(request.InsuranceProviderFiles.ToList(), insuranceProvider);
 
-                    if (insuranceProviderDocuments.Count > 0)
-                    {
-                        await _unitOfWork.InsuranceProviderDocumentRepository.AddRangeAsync(insuranceProviderDocuments, cancellationToken);
+                if (insuranceProviderDocuments.Count > 0)
+                {
+                    await _unitOfWork.InsuranceProviderDocumentRepository.AddRangeAsync(insuranceProviderDocuments,
+                        cancellationToken);
 
-                        await _unitOfWork.SaveChangesAsync(cancellationToken);
-                    }
-                }
+                    await _unitOfWork.SaveChangesAsync(cancellationToken);
+                }
+            }
 
-                await transaction.CommitAsync(cancellationToken);
+            await transaction.CommitAsync(cancellationToken);
 
-                return new CreateInsuranceProviderResponse()
-                {
-                    InsuranceProviderId = insuranceProvider.Id,
-                    Success = true
-                };
-            }
-            catch (Exception ex)
-            {
-                await _logger.LogErrorAsync(insuranceProvider, ex, ApplicationConstants.CommorError, cancellationToken);
+            return new CreateInsuranceProviderResponse
+            {
+                InsuranceProviderId = insuranceProvider.Id,
+                Success = true
+            };
+        }
+        catch (Exception ex)
+        {
+            await _logger.LogErrorAsync(insuranceProvider, ex, ApplicationConstants.CommorError, cancellationToken);
 
-                await transaction.RollbackAsync(cancellationToken);
+            await transaction.RollbackAsync(cancellationToken);
 
-                return new CreateInsuranceProviderResponse()
-                {
-                    Success = false,
-                    FailureReason = ex.Message
-                };
-            }
-        }
+            return new CreateInsuranceProviderResponse
+            {
+                Success = false,
+                FailureReason = ex.Message
+            };
+        }
+    }
 
-        private async Task<List<InsuranceProviderDocument>> UploadInsuranceProviderFiles(List<InsuranceProviderFiles> InsuranceProviderFiles, InsuranceProvider insuranceProvider)
-        {
-
-            List<InsuranceProviderDocument> insuranceProviderDocuments = new List<InsuranceProviderDocument>();
-
-            try
-            {
+    private async Task<List<InsuranceProviderDocument>> UploadInsuranceProviderFiles(
+        List<InsuranceProviderFiles> InsuranceProviderFiles, InsuranceProvider insuranceProvider)
+    {
+        List<InsuranceProviderDocument> insuranceProviderDocuments = new();
 
-                foreach (var file in InsuranceProviderFiles)
-                {
-                    if (file.DocumentData != null)
-                    {
-                        AzureDocument document = new AzureDocument();
-                        document.DocumentName = $"{insuranceProvider.Id}_{file.DocumentName}";
-                        document.DocumentData = Convert.FromBase64String(file.DocumentData);
-                        document.MainDirectory = ApplicationConstants.AzureMainDirectory;
-                        document.ChildDirectory = insuranceProvider.Id.ToString();
+        foreach (var file in InsuranceProviderFiles)
+            if (file.DocumentData != null)
+            {
+                var document = new AzureDocument();
+                document.DocumentName = $"{insuranceProvider.Id}_{file.DocumentName}";
+                document.DocumentData = Convert.FromBase64String(file.DocumentData);
+                document.MainDirectory = ApplicationConstants.AzureMainDirectory;
+                document.ChildDirectory = insuranceProvider.Id.ToString();
 
-                        var documentPath = await _azureFileStorageConnection.UploadInsuranceProviderFiles(document);
+                var documentPath = await _azureFileStorageConnection.UploadInsuranceProviderFiles(document);
 
-                        if (documentPath != null)
-                        {
-                            InsuranceProviderDocument insuranceProviderDocument = new InsuranceProviderDocument();
-                            insuranceProviderDocument.InsurerId = insuranceProvider.Id;
-                            insuranceProviderDocument.DocumentName = file.DocumentName;
-                            insuranceProviderDocument.DocumentPath = documentPath;
-                            insuranceProviderDocument.IsActive = true;
-                            insuranceProviderDocument.CreatedBy = insuranceProviderDocument.ModifiedBy = insuranceProvider.InsurerName;
-                            insuranceProviderDocument.CreatedDate = DateTime.UtcNow;
-                            insuranceProviderDocument.ModifiedDate = DateTime.UtcNow;
+                if (documentPath != null)
+                {
+                    var insuranceProviderDocument = new InsuranceProviderDocument();
+                    insuranceProviderDocument.InsurerId = insuranceProvider.Id;
+                    insuranceProviderDocument.DocumentName = file.DocumentName;
+                    insuranceProviderDocument.DocumentPath = documentPath;
+                    insuranceProviderDocument.IsActive = true;
+                    insuranceProviderDocument.CreatedBy =
+                        insuranceProviderDocument.ModifiedBy = insuranceProvider.InsurerName;
+                    insuranceProviderDocument.CreatedDate = DateTime.UtcNow;
+                    insuranceProviderDocument.ModifiedDate = DateTime.UtcNow;
 
-                            insuranceProviderDocuments.Add(insuranceProviderDocument);
-                        }
-
-                    }
+                    insuranceProviderDocuments.Add(insuranceProviderDocument);
                 }
             }
-            catch (Exception ex)
-            {
-                throw;
-            }
 
-            return insuranceProviderDocuments;
-        }
+        return insuranceProviderDocuments;
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Services/Implementations/CropCategoryService.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing eSusInsurers.Domain.Entities;\nusing eSusInsurers.Infrastructure.Common;\nusing eSusInsurers.Models.enums;\nusing eSusInsurers.Models.Helpers;\nusing eSusInsurers.Models;\nusing eSusInsurers.Services.Common;\nusing System.Linq.Expressions;\nusing AutoMapper.QueryableExtensions;\nusing Microsoft.EntityFrameworkCore;\n\nnamespace eSusInsurers.Services\n{\n    public class CropCategoryService(IUnitOfWork unitOfWork, IMapper mapper) : ICropCategoryService\n    {\n        public async Task<List<CropCategoryModel>?> GetCropCategories(CancellationToken cancellationToken)\n        {\n            Dictionary<string, Models.Common.Filter> filters = CropCategoriesFilter();\n            Expression<Func<CropCategory, bool>> predicate = ExpressionBuilder<CropCategory>.BuildFilterExpression(filters);\n\n            var query = unitOfWork.CropCategoryRepository.GetAll()\n               .Where(predicate)\n               .ProjectTo<CropCategoryModel>(mapper.ConfigurationProvider);\n\n            return await query.ToListAsync(cancellationToken);\n        }\n\n        #region Private Methods\n        private static Dictionary<string, Models.Common.Filter> CropCategoriesFilter()\n        {\n            var filters = new Dictionary<string, Models.Common.Filter>();\n\n            Filters.AddFilterIfNotEmpty(filters, \"True\", \"IsActive\", SearchOperationEnum.Equal);\n\n            return filters;\n        } \n        #endregion\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Services/Implementations/CropCategoryService.cs b/eSusInsurers/Services/Implementations/CropCategoryService.cs
--- a/eSusInsurers/Services/Implementations/CropCategoryService.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Services/Implementations/CropCategoryService.cs	(date 1732264029119)
@@ -1,39 +1,40 @@
+using System.Linq.Expressions;
 using AutoMapper;
+using AutoMapper.QueryableExtensions;
 using eSusInsurers.Domain.Entities;
 using eSusInsurers.Infrastructure.Common;
+using eSusInsurers.Models;
 using eSusInsurers.Models.enums;
 using eSusInsurers.Models.Helpers;
-using eSusInsurers.Models;
 using eSusInsurers.Services.Common;
-using System.Linq.Expressions;
-using AutoMapper.QueryableExtensions;
 using Microsoft.EntityFrameworkCore;
 
-namespace eSusInsurers.Services
-{
-    public class CropCategoryService(IUnitOfWork unitOfWork, IMapper mapper) : ICropCategoryService
-    {
-        public async Task<List<CropCategoryModel>?> GetCropCategories(CancellationToken cancellationToken)
-        {
-            Dictionary<string, Models.Common.Filter> filters = CropCategoriesFilter();
-            Expression<Func<CropCategory, bool>> predicate = ExpressionBuilder<CropCategory>.BuildFilterExpression(filters);
+namespace eSusInsurers.Services;
+
+public class CropCategoryService(IUnitOfWork unitOfWork, IMapper mapper) : ICropCategoryService
+{
+    public async Task<List<CropCategoryModel>?> GetCropCategories(CancellationToken cancellationToken)
+    {
+        Dictionary<string, Models.Common.Filter> filters = CropCategoriesFilter();
+        Expression<Func<CropCategory, bool>> predicate = ExpressionBuilder<CropCategory>.BuildFilterExpression(filters);
 
-            var query = unitOfWork.CropCategoryRepository.GetAll()
-               .Where(predicate)
-               .ProjectTo<CropCategoryModel>(mapper.ConfigurationProvider);
+        var query = unitOfWork.CropCategoryRepository.GetAll()
+            .Where(predicate)
+            .ProjectTo<CropCategoryModel>(mapper.ConfigurationProvider);
 
-            return await query.ToListAsync(cancellationToken);
-        }
+        return await query.ToListAsync(cancellationToken);
+    }
 
-        #region Private Methods
-        private static Dictionary<string, Models.Common.Filter> CropCategoriesFilter()
-        {
-            var filters = new Dictionary<string, Models.Common.Filter>();
+    #region Private Methods
+
+    private static Dictionary<string, Models.Common.Filter> CropCategoriesFilter()
+    {
+        var filters = new Dictionary<string, Models.Common.Filter>();
 
-            Filters.AddFilterIfNotEmpty(filters, "True", "IsActive", SearchOperationEnum.Equal);
+        Filters.AddFilterIfNotEmpty(filters, "True", "IsActive", SearchOperationEnum.Equal);
 
-            return filters;
-        } 
-        #endregion
-    }
-}
+        return filters;
+    }
+
+    #endregion
+}
\ No newline at end of file
Index: eSusInsurers/Models/Countries/SubCountiesModel.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing eSusInsurers.Common.Mappings;\nusing eSusInsurers.Domain.Entities;\n\nnamespace eSusInsurers.Models.Countries\n{\n    public class SubCountiesModel : IMapFrom<SubCounty>\n    {\n        public int SubCountyId { get; set; }\n        public string SubCountyName { get; set; }\n        public int DistrictId { get; set; }\n\n        public void Mapping(Profile profile)\n        {\n            profile.CreateMap<SubCounty, SubCountiesModel>()\n                                .ForMember(d => d.SubCountyId, opt => opt.MapFrom(c => c.Id));\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Models/Countries/SubCountiesModel.cs b/eSusInsurers/Models/Countries/SubCountiesModel.cs
--- a/eSusInsurers/Models/Countries/SubCountiesModel.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Models/Countries/SubCountiesModel.cs	(date 1732264029269)
@@ -2,18 +2,17 @@
 using eSusInsurers.Common.Mappings;
 using eSusInsurers.Domain.Entities;
 
-namespace eSusInsurers.Models.Countries
-{
-    public class SubCountiesModel : IMapFrom<SubCounty>
-    {
-        public int SubCountyId { get; set; }
-        public string SubCountyName { get; set; }
-        public int DistrictId { get; set; }
+namespace eSusInsurers.Models.Countries;
+
+public class SubCountiesModel : IMapFrom<SubCounty>
+{
+    public int SubCountyId { get; set; }
+    public string SubCountyName { get; set; }
+    public int DistrictId { get; set; }
 
-        public void Mapping(Profile profile)
-        {
-            profile.CreateMap<SubCounty, SubCountiesModel>()
-                                .ForMember(d => d.SubCountyId, opt => opt.MapFrom(c => c.Id));
-        }
+    public void Mapping(Profile profile)
+    {
+        profile.CreateMap<SubCounty, SubCountiesModel>()
+            .ForMember(d => d.SubCountyId, opt => opt.MapFrom(c => c.Id));
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Models/Countries/ParishModel.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using AutoMapper;\nusing eSusInsurers.Common.Mappings;\nusing eSusInsurers.Domain.Entities;\n\nnamespace eSusInsurers.Models.Countries\n{\n    public class ParishModel : IMapFrom<Parish>\n    {\n        public int ParishId { get; set; }\n        public string ParishName { get; set; }\n\n        public void Mapping(Profile profile)\n        {\n            profile.CreateMap<Parish, ParishModel>()\n                                .ForMember(d => d.ParishId, opt => opt.MapFrom(c => c.Id));\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Models/Countries/ParishModel.cs b/eSusInsurers/Models/Countries/ParishModel.cs
--- a/eSusInsurers/Models/Countries/ParishModel.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Models/Countries/ParishModel.cs	(date 1732264029274)
@@ -2,17 +2,16 @@
 using eSusInsurers.Common.Mappings;
 using eSusInsurers.Domain.Entities;
 
-namespace eSusInsurers.Models.Countries
-{
-    public class ParishModel : IMapFrom<Parish>
-    {
-        public int ParishId { get; set; }
-        public string ParishName { get; set; }
+namespace eSusInsurers.Models.Countries;
+
+public class ParishModel : IMapFrom<Parish>
+{
+    public int ParishId { get; set; }
+    public string ParishName { get; set; }
 
-        public void Mapping(Profile profile)
-        {
-            profile.CreateMap<Parish, ParishModel>()
-                                .ForMember(d => d.ParishId, opt => opt.MapFrom(c => c.Id));
-        }
+    public void Mapping(Profile profile)
+    {
+        profile.CreateMap<Parish, ParishModel>()
+            .ForMember(d => d.ParishId, opt => opt.MapFrom(c => c.Id));
     }
-}
+}
\ No newline at end of file
Index: eSusInsurers/Controllers/BaseController.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using eSusInsurers.Filter;\nusing Microsoft.AspNetCore.Mvc;\n\nnamespace eSusInsurers.Controllers\n{\n    [ApiController]\n    [ApiExceptionFilter]\n    [ProducesResponseType(StatusCodes.Status400BadRequest)]\n    [ProducesResponseType(StatusCodes.Status403Forbidden)]\n    [ProducesResponseType(StatusCodes.Status429TooManyRequests)]\n    [ProducesResponseType(StatusCodes.Status500InternalServerError)]\n    [ProducesResponseType(StatusCodes.Status503ServiceUnavailable)]\n    public class BaseController : ControllerBase\n    {\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/eSusInsurers/Controllers/BaseController.cs b/eSusInsurers/Controllers/BaseController.cs
--- a/eSusInsurers/Controllers/BaseController.cs	(revision a3578148ccd7ad8de03a4270ab3f356021e80115)
+++ b/eSusInsurers/Controllers/BaseController.cs	(date 1732264029109)
@@ -1,16 +1,15 @@
 using eSusInsurers.Filter;
 using Microsoft.AspNetCore.Mvc;
 
-namespace eSusInsurers.Controllers
-{
-    [ApiController]
-    [ApiExceptionFilter]
-    [ProducesResponseType(StatusCodes.Status400BadRequest)]
-    [ProducesResponseType(StatusCodes.Status403Forbidden)]
-    [ProducesResponseType(StatusCodes.Status429TooManyRequests)]
-    [ProducesResponseType(StatusCodes.Status500InternalServerError)]
-    [ProducesResponseType(StatusCodes.Status503ServiceUnavailable)]
-    public class BaseController : ControllerBase
-    {
-    }
-}
+namespace eSusInsurers.Controllers;
+
+[ApiController]
+[ApiExceptionFilter]
+[ProducesResponseType(StatusCodes.Status400BadRequest)]
+[ProducesResponseType(StatusCodes.Status403Forbidden)]
+[ProducesResponseType(StatusCodes.Status429TooManyRequests)]
+[ProducesResponseType(StatusCodes.Status500InternalServerError)]
+[ProducesResponseType(StatusCodes.Status503ServiceUnavailable)]
+public class BaseController : ControllerBase
+{
+}
\ No newline at end of file
